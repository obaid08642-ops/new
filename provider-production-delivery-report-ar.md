# تقرير تسليم تنفيذ تطبيق مزوّدي الخدمات

**التاريخ:** 27 أغسطس 2026  
**فرع التنفيذ:** `remediation/provider-production-governed`  
**المرجع الحاكم:** `main`  
**المصدر الانتقائي فقط:** `origin/quarantine/workstation-source-51a84c7`

> **ملاحظة استعادة مهمة:** كانت الالتزامات التاريخية المذكورة في النسخة الأولية من هذا التقرير محلية ولم تُدفع إلى GitHub قبل إعادة ضبط بيئة العمل؛ لذلك لا يمكن للمراجع العثور على SHA السابق في البعيد. مرجع المراجعة الصحيح الآن هو فرع الاستعادة المنشور، والأرشيفات المحفوظة فيه، وملفات الفرق النصية تحت `review/`.

## الخلاصة التنفيذية

اكتمل تنفيذ مسار الإصلاح المحلي على فرع مستقل من `main`، مع استخدام الفرع الحجري مصدراً انتقائياً للعقود والمنطق والاختبارات، دون دمجه. أزيلت مسارات تشغيل وهمية محددة، ورُبطت الشاشات ذات الصلة بخدمات خادم فعلية، وأضيفت واجهات خادم مفقودة وتحقق ملكية أساسي لا سيما للإحالات. كما أضيفت بوابات ثابتة لفحص بيانات وقت التشغيل، وعقود تطبيق المزوّد، وسيناريوهات تكامل محمولة تتطلب بيئة اختبار صريحة.

> لا تمثل هذه النتيجة تصريح نشر إنتاجي. نجحت اختبارات البناء والوحدة والعقود محلياً، لكن لم يُشغّل تكامل حي ضد بيئة MongoDB/Redis/LiveKit/S3 معزولة، ولم يُشغّل ترحيل بيانات المستشفى على قاعدة فعلية.

| المحور | حالة التنفيذ المحلي | ملاحظة حاكمة |
|---|---|---|
| إزالة بيانات التشغيل الوهمية ضمن المسارات التي عولجت | مكتمل ومفحوص | اجتازت بوابة `RUNTIME_DATA_GATE`. |
| تطبيق المزوّد Expo/React Native | مكتمل بناءً وعقوداً | فحص TypeScript وعقد إصدار التطبيق نجحا. |
| خادم NestJS/Mongoose | مكتمل بناءً واختبارات وحدة | مجموعة الخادم الكاملة نجحت: 90 مجموعة و483 اختباراً. |
| الإحالات والشبكة والعروض | مربوط ومتحقق من الملكية | الإحالة تتحقق من المريض ومن الموعد عند وروده ومن الوجهة المعتمدة عند تحديدها. |
| E2E حي | غير منفذ | السيناريوهات أصبحت محمولة ورفضت التشغيل غير المصرح به، لكنها لم تُشغّل على خدمات حية. |
| النشر أو الدمج إلى `main` | غير منفذ | يظل فرع التنفيذ مستقلاً لحين استكمال أدلة عدم الجاهزية. |

## ما نُفّذ

### العقود والحوكمة

أضيفت حزمة `packages/shared-contracts` لتوصيف آلات الحالة ومسارات القطاعات السبعة: الطبيب، الصيدلية، المختبر، الأشعة، التمريض، المنشأة، والإسعاف. كما أضيفت بوابة `scripts/check-provider-runtime.js` التي تفحص بصفة دلالية ما إذا بقيت بيانات تشغيل demo/mock/fallback في الشاشات والمسارات التي تم إصلاحها.

### تطبيق المزوّد

| القطاع أو الشاشة | التنفيذ الفعلي |
|---|---|
| الطبيب | تم ربط قائمة المواعيد والتقارير الواردة، وCRM، والإحالات، والتوفر بخدمات الخادم. كما استُخدمت غرفة مكالمة فعلية بدلاً من غلاف فيديو مبسط. |
| الصيدلية | تم ربط بث الطلبات بـSocket.IO مع JWT واستعلام احتياطي، وإجراءات قبول كامل أو جزئي أو رفض بمسارات الخادم، وإتاحة ماسح الباركود من Expo Camera للتحقق من المخزون. |
| المختبر | أصبحت مراجعة النتيجة ترسل `structuredData` إلى واجهة رفع التقرير. أزيلت شاشة إدخال بديلة كانت ترسل اسماً ثابتاً لملف PDF وتُظهر نجاحاً غير موثق. |
| الأشعة | أصبح اختيار PDF/DICOM يتم عبر Document Picker ثم يرفع إلى التخزين الخاص باستخدام `ProviderApi.uploadFile` قبل إرسال معرّفات الكائنات إلى مسار التقرير. |
| المنشأة والمستشفى | أصبحت إدارة الشيفتات تقرأ من `/facility/shifts` وتعيد التحميل بعد طلب البديل. كما صُححت صلة مستخدمي المستشفى بحساب المنشأة مع مسار ترحيل مستقل. |
| الإسعاف والتمريض | أُدرج مسار الإسعاف في التطبيق، واعتمدت المسارات القائمة ذات الخلفية الفعلية في التمريض والإسعاف. |
| الإعدادات والصفحات المشتركة | تم ربط شبكة الإحالات والعروض وملف الموقع العام بـAPI حقيقي. أزيلت تواريخ عروض منتهية وميزانية ومدّة افتراضيتان من وقت التشغيل. |

### الخادم والملكية

تسجل وحدة `provider-production` مسارات القرار التأميني، CRM، الإحالات، شبكة الإحالات، العروض، قائمة الفنيين، المطالبات، التقارير الواردة، والتوفر. وصارت شبكة الإحالات تقرأ حقلي نوع الملف التعريفي الحديث والقديم، ولا تعرض إلا الجهات المرتبطة بحسابات حالتها `approved` أو `active`.

إنشاء الإحالة لم يعد يثق باسم المريض أو اسم الوجهة الواردين من العميل. يتحقق الخادم من وجود المريض ودوره، ويتحقق عند تقديم رقم موعد من تطابق المريض وملكية الطبيب، ويتحقق عند تقديم معرف وجهة من أن ملفها من النوع المطلوب وأن حسابها معتمد أو نشط. ويُعاد اشتقاق الاسم من سجلات الخادم.

العروض المنشأة تبقى بحالة `pending_review`. لا يوجد في النسخة الحالية جدول أسعار تسويقي مركزي مذكور في خطة المستخدم، لذلك لا ينبغي اعتمادها كمبالغ نهائية أو تحصيلها قبل قرار الإدارة أو قبل إقرار نموذج تسعير مركزي مرتبط بالكتالوج.

## اللقطات البرمجية المحفوظة

كل تعديلات التطبيق والخادم محفوظة داخل الأرشيفين المتتبعين في Git، لأن مصادر `main` تُدار بهذه الصيغة:

| الأرشيف | محتواه |
|---|---|
| `NabdProvider-provider.zip` | تطبيق React Native/Expo بعد التعديلات والعقود المحلية. |
| `nabdah-backend.zip` | خادم NestJS، الاختبارات المضافة، وسيناريوهات E2E المحمولة. |

كانت التغييرات قد نُفذت سابقاً ضمن بيئة محلية ثم فُقدت بيانات Git الخاصة بها قبل دفعها. استُعيدت الآن من الأرشيفات نفسها إلى فرع مراجعة جديد مبني من `main`. ولأن أرشيفات المصدر ملفات ثنائية، أضيفت أيضاً فروقات نصية قابلة للمراجعة في `review/provider-source-recovery.patch` و`review/backend-source-recovery.patch`. يجب أن يعتمد المراجع الالتزام الجديد المنشور لا أي SHA تاريخي محلي غير متاح.

## أدلة التحقق المنفذة

| الفحص | النتيجة |
|---|---|
| `packages/shared-contracts`: فحص النوع والبناء والاختبار | نجح؛ 7 اختبارات عقود. |
| تطبيق المزوّد: `npx tsc --noEmit` | نجح بعد كل تعديل نهائي ذي صلة. |
| عقد إصدار التطبيق `provider-app.contracts.test.js` | نجح؛ 10 اختبارات تشمل الطبيب، الصيدلية، المختبر، الأشعة، المنشأة، الإحالات، العروض، التوفر، والخرائط. |
| اختبارات المختبر والأشعة | نجحت؛ 4 مجموعات و24 اختباراً. |
| اختبارات `provider-production` للمسارات والتوفر والإحالات والعروض | نجحت؛ 3 مجموعات و9 اختبارات في آخر تشغيل مركز. |
| بناء الخادم `npm run build` | نجح. |
| مجموعة الخادم الكاملة `npm test -- --runInBand` | نجحت؛ 90 مجموعة و483 اختباراً. |
| بوابة بيانات وقت التشغيل | `RUNTIME_DATA_GATE=PASS`. |
| فحص قابلية نقل E2E | نجح فحص الصياغة وفحص الرفض المسبق؛ لم يُنفذ سيناريو حي. |

تحمل اختبارات الخادم تحذيرات متوقعة في بيئة الاختبار، منها غياب إعدادات S3 في اختبار وحدة التخزين، ورفض webhook عند غياب سره، ورسائل مسار اختبار Excel غير الصالح. لم تكن تلك إخفاقات؛ اجتازت المجموعات جميعاً.

## سيناريوهات التكامل الحية

نُقلت سيناريوهات `provider-production.js` و`pharmacy-scenarios.js` و`provider-verticals.js` ومشغّلها إلى `backend/e2e`. أزيلت المسارات المطلقة، والاعتماد على `/tmp/e2e/boot.ok`، والعناوين المحلية الافتراضية، والاعتماد المطلق على `jsonwebtoken`. أصبح التنفيذ يتطلب متغيرات صريحة هي `BASE` و`MONGO_URI` و`E2E_DB` و`E2E_JWT_SECRET` و`E2E_ALLOW_DESTRUCTIVE=true`، إضافة إلى `PROVIDER_APP` عند استخدام مشغّل البوابات.

تتحقق هذه السيناريوهات من بيانات التخزين وتغيرها، ولهذا يجب تشغيلها على قاعدة اختبار مخصصة قابلة للحذف فقط. تم فحص رفضها قبل توفير متغيرات البيئة، وليس تشغيلها على قاعدة بيانات أو خادم حي.

## عناصر عدم الجاهزية للنشر (NO-GO)

| الدليل الناقص | الإجراء المطلوب قبل النشر |
|---|---|
| ترحيل `hospital_staff.parent_account_id` إلى `parent_provider_account_id` | أخذ نسخة احتياطية، وتشغيل وضع المعاينة، والتحقق من الأعداد، ثم التطبيق الصريح فقط في بيئة مرحلية أو إنتاجية مصرح بها. |
| تكامل حي للخادم | تجهيز MongoDB وRedis معزولين وLiveKit وتخزين كائنات ومفاتيح اختبار، ثم تشغيل سيناريوهات E2E المحمولة وتسجيل نتيجتها. |
| التخزين الخاص للأشعة والمختبر | رفع وتنزيل ملف PDF وDICOM حقيقيين والتحقق من الصلاحيات والمراجع ومدة الاحتفاظ. |
| الاتصال الفوري والأجهزة | التحقق من Socket.IO، إشعارات push، Expo Camera، Document Picker، الخرائط، وتجربة التنقل على جهاز Android وiOS فعليين. |
| المكالمات المرئية | إنشاء جلسة LiveKit حية، الانضمام من مزود ومريض، والتحقق من الصلاحيات والانقطاع والاسترداد. |
| الهوية والرسائل | اختبار OTP ومزود الرسائل الفعليين، بما في ذلك معدلات الإرسال والتدقيق ومعالجة الأعطال. |
| نموذج تسعير العروض | اعتماد كتالوج أو rate card خادمي إذا كان المطلوب منع إدخال السعر من واجهة المزوّد، ثم ربط العرض بعنصر وموافقة إدارية قبل أي تحصيل. |

## خطوات مقترحة للمرحلة التالية

ابدأ ببيئة مرحلية معزولة ونسخة احتياطية. شغّل ترحيل موظفي المستشفى أولاً بوضع المعاينة، وطابق عدد السجلات القابلة للترحيل والمستبعدة، ثم طبّق الترحيل بعد موافقة المسؤول. بعد ذلك عيّن متغيرات E2E الصريحة وشغّل السيناريوهات الثلاثة ومشغّل البوابات، مع الاحتفاظ بالمخرجات.

إذا نجحت الأدلة الحية، نفّذ اختبار قبول يدوي على جهازين فعليين لكل قطاع من القطاعات السبعة، ثم راجع سجلات التدقيق وعمليات الفشل. عندها فقط يكون من المناسب إنشاء مراجعة للفرع وطلب دمجه إلى `main`؛ لا ينبغي دمج الفرع أو نشره قبل ذلك.
