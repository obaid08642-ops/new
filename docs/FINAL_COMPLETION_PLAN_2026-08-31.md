# خطة الإكمال النهائي — 2026-08-31 (بعد جلسة الصدق الكامل)
قاعدة كل مرحلة: نفّذ → راجع آلياً → أكّد → ارفع → تحقق SHA.

## المرحلة 9 — التحول البصري الفعلي (ليس رموزاً بجانب القديمة)
- 9.1 تحويل القيم الحية في colors.ts: p/pd/ps/pt → ليموني brand (#B8E030/#7CB518/#F0F9D4/#3F6212) في light+dark — تغيير مركزي واحد ينتشر في كل الشاشات التي تقرأ النظام.
- 9.2 محو أثر التغيير: فحص التباين الأساسي (نص داكن على ليموني) + توثيق.
- 9.3 الأيقونات: جرد مكوّن Icon الحالي؛ لا استبدال أعمى — توثيق القرار (الأيقونات الحالية MaterialSymbolsRounded — تحويلها لفيكتور 3D مخصص = أصول تصميم، تُنتج لاحقاً أو بأداة تصميم).

## المرحلة 10 — شاشات الويب الناقصة الحقيقية (لا تكافؤ ادعائي)
لكل مجال: صفحات فرعية حقيقية بعقود موجودة (backend controllers مؤكدة):
- 10.1 الصحة: /health/vitals موجودة ✅ — إكمال الناقص من القائمة الفعلية (reports/timeline، ai-analysis) بعقود medical-reports الموجودة.
- 10.2 التغذية: /nutrition موجودة كصفحة واحدة — بناء الفرعيات ذات العقود المؤكدة (nutrition.controller موجود: plans/tracker).
- 10.3 الحمل: /maternity موجودة — بناء الفرعيات بعقود maternity.controller (profile موجود).
- 10.4 الإعدادات: /settings موجودة — بناء الفرعيات (security/notifications/language) بعقود users الموجودة.
- 10.5 الدعم: /support موجودة — بناء ticket.
- القاعدة: لا شاشة بدون عقد مؤكد. ما لا عقد له → يُوثق ولا يُختلق.

## المرحلة 11 — تطبيق المزود: التحقق السيناريوي الكامل
- 11.1 لكل تخصص: قائمة إجراءات إتمام الطلب (accept/complete/…) في الشاشة مقابل endpoint الباكند — مصفوفة حقيقية سطر-بسطر.
- 11.2 سد أي فجوة تظهر (ربط زر بعقدة موجودة، أو توثيق غياب العقدة).
- 11.3 فحص البيانات الوهمية داخل الشاشات الـ 39 (مصفوفات ثابتة/mock markers) وإزالتها أو توثيق مشروعيتها.

## المرحلة 12 — بوابة CI (إلزامية، خارج هذه البيئة)
- npm ci --legacy-peer-deps && npm i express-mongo-sanitize && check && test && build لكل مشروع.
- لا دمج إلى main إلا بإشارة المستخدم بعد خضور البوابة.

## حالة التنفيذ
| المرحلة | الحالة |
|---|---|
| 9 | ⏳ تبدأ الآن |
| 10-12 | ⏳ تباعاً |
