# المرحلة F — SEO/GEO/AEO + AI

## الحالي
- `llms.txt` موجود + 6 لغات + sitemaps + Product structured data ✅
- FAQ/Speakable/Breadcrumb موجود جزئياً

## المطلوب
1. FAQPage + Speakable لكل مقال/منتج (JSON-LD)
2. قياس ترتيب + اقتباسات LLMs (Perplexity/ChatGPT) — سكريبت weekly crawl
3. Agent skills (`/.well-known/ai-catalog.json` موجود ✅)

## التالي
- إضافة FAQ JSON-LD لكل `/p/{slug}` + `/articles/{id}`
- سكريبت `measure-llm-citations.js` weekly

مؤجل لما بعد D — لا يمنع الإطلاق.
