/**
 * Module Index Template
 * Each feature module follows this structure and exports its public API here.
 *
 * Structure:
 *   screens/     — Route-level screens (only navigated to via expo-router)
 *   components/  — Module-specific reusable components
 *   hooks/       — Module-specific hooks
 *   store/       — Redux slice (if needed)
 *   repository/  — Data access layer (implements Repository<T>)
 *   types/       — Module-specific TypeScript types
 *
 * DO NOT import directly from sub-folders outside this module.
 * Only import from this index.ts file.
 */

// ── Public API ──────────────────────────────────────────────────────────────
// Export components, hooks, types when implemented.
// Example:
//   export { PharmacyScreen } from './screens/PharmacyScreen';
//   export { usePharmacy }    from './hooks/usePharmacy';
//   export type { Product }   from './types';

export {}; // Module placeholder — implementation in Phase 3
