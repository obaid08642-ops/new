/**
 * Backward-compatible API service entry point.
 * Keeps legacy screens on the authenticated client implemented in utils/api.
 */
export { apiFetch, BASE_URL, FASTAPI_BASE_URL, R2_PUBLIC_URL } from '../utils/api';
