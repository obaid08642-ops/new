# Verifier Index — مشروع نبض (الفحص الشامل)

## v1 — 2026-08-02
**ما يقيسه:** الجرد الشامل + فحص الصحة الأساسي للمشروع:
- عدد الشاشات الفعلي لكل مكوّن مقابل المُعلن (مريض 239 / مزود 114 / أدمن 23 / باك إند 1122 route / 97 module)
- صفر TODO/Placeholder/Mock/Dummy في كود الإنتاج
- كل استدعاء API في الواجهات له route مطابق في الباك إند
- فحص secrets/tokens المسرّبة داخل الكود
- صحة التشغيل الحي: health endpoint على api.nabd.plus

**كيف يعمل:** سكربت `verifier/v1/verify.sh` يُنتج تقرير `verifier/runs/<timestamp>.log` بنتائج PASS/FAIL.
**الفرق عن السابق:** أول نسخة.

## Run 20260802T005956Z — بعد R2 (commit 88caeec)
**النتيجة الخام:** FAIL حسب السكربت — لكن التحليل اليدوي للمواضع المُبلّغة:
- **مزود (315):** 304 = خاصية `placeholder=` القياسية في React Native + 11 موضعًا كلها حميدة (تعريفات types، تعليقات صادقة عن ميزات قيد التطوير). صفر بيانات وهمية فعلية.
- **باك إند (28):** 26 موضعًا كلها حميدة: مطابقات /todo/i على المعرّف `toDomain`، نصوص i18n، تعليقات توثيقية. مفاتيح Apple App Attest غير مُهيأة والنظام يُبلّغ `not_configured` بصدق (بند مالك).
- **مريض (217):** 140 خاصية placeholder قياسية + **77 موضعًا مشبوهًا تحتاج مراجعة** — هدف مرحلة رحلة المريض القادمة.
- **Secrets (8):** كلها نفس مفتاح Firebase في google-services.json (تصميمي داخل التطبيقات — يحتاج تقييد App Check، موثق كبند مالك). لا توكنات GitHub/AWS/مفاتيح خاصة.
- **API الحي:** 200 OK.
- **عدّادات v1 الخام** (routes 639، شاشات مزود 51 ملفًا) تقيس بطريقة مختلفة عن المُعلن — تُعاير في v2.
**الخلاصة:** R2 نظّف المزود والباك إند بالكامل؛ التلوث المتبقي الحقيقي متركز في تطبيق المريض (77 موضعًا).

## Run بعد R3 (commit f42795e) — مراجعات الوحدات
**ما تغيّر:**
- مختبر: زر «فتح الكاميرا» كان يولّد باركود عشوائي → ماسح كاميرا حقيقي (expo-camera/CameraView).
- أشعة: التطبيق ينادي 8 مسارات غير موجودة في الباك إند (checkin/start-scan/abort/upload-report/submit-report-for-review/approve-report/insurance-approval/reschedule) → أُضيفت كلها في RadiologyController فوق خدمات حقيقية موجودة (state machine + refund event).
- تمريض: GPS كان يُرسل إحداثيات وهمية (24.7136+jitter) → expo-location حقيقي مع رفض صادق عند منع الصلاحية؛ نقطة تتبع الزيارة في الباك إند كانت تُرجع هاتفًا وهميًا +966500000000 وETA=15 ثابت → قيم null صادقة + ETA محسوب (haversine/30kmh)؛ خطط الرعاية كانت قالبًا ثابتًا → مسارا GET/POST /home-care/care-plans/:patientId + شاشة حقيقية؛ الملاحظات اليومية كانت ثابتة → مسارا /nursing/notes + جلب/حفظ حقيقي بمعرّف المريض؛ محفظة التمريض كانت أرقامًا ثابتة (4,017 وأسماء مرضى وهمية) → /provider/wallet + /provider/payouts/request الحقيقيان؛ إنهاء الزيارة من القائمة يمر الآن عبر تقرير الزيارة الحقيقي.
- منشأة: مسح QR للحضور كان toast → كاميرا حقيقية + PATCH /care/appointments/:id/check-in؛ التحديث اليدوي كان setTimeout → إعادة جلب فعلية؛ التعاميم والموارد كانتا مصفوفات محلية → مسارات /facility/announcements و/facility/resources جديدة + شاشتان معاد بناؤهما؛ ملف المنشأة كان حفظًا وهميًا ببيانات ثابتة → GET/PATCH /provider/profile حقيقي (+إضافة sub_specialties للقائمة البيضاء)؛ قبول/رفض طلب المنشأة → /provider/jobs/:type/:id/accept|reject.
- إسعاف: تحقق — كل المسارات الستة حقيقية مسبقًا، لا تغيير.
**المواضع المشبوهة الحقيقية المتبقية:** مزود = 0، باك إند = 0 (28 خام كلها حميدة)، مريض = 77 (المرحلة القادمة).

## Run بعد R5 (commit b421bcf) — تطهير خدمات الباك إند الوهمية + بطاقة الهوية
**ما تغيّر (باك إند):**
- health.service: 7 نقاط كانت تُرجع بيانات ثابتة مزيّفة (تقارير/وصفات/تذكيرات/جهات طوارئ/أمراض مزمنة/أدوية مزمنة/اتجاهات) → كلها الآن من قواعد البيانات: التقارير من medicalreports، الوصفات من prescriptions مع حل اسم الطبيب من users، التذكيرات من reminders مع التزام اليوم من السجل، الاتجاهات تُحسب من قراءات المريض الفعلية فقط، جهات الطوارئ CRUD حقيقي مضمّن في patient_profiles (+مسارا POST/DELETE جديدان).
- users.service: إعدادات الإشعارات/الخصوصية/الأمان كانت تُرجع ثوابت ولا تحفظ شيئًا → تُخزَّن في patient_profiles؛ تغيير كلمة المرور كان يرجع نجاحًا وهميًا → تحقق bcrypt حقيقي وتدوير؛ الجلسات من Redis (refresh_user)؛ التخزين يُحسب من المحتوى الفعلي.
- إصلاح صامت خطير: التطبيق يرسل chronic_conditions بينما المخطط chronic_diseases — كانت تُحذف بصمت بسبب strict mode → ربط الاثنين في الخدمة + إضافة الحقول الناقصة (full_name/dob/national_id/إعدادات) للمخطط.
- ai.service: فشل تحليل الوجبة كان يُرجع قيمًا غذائية صفرية مختلقة → خطأ صادق.
**ما تغيّر (مريض):**
- بطاقة الهوية الصحية: كانت هوية مختلقة بالكامل (اسم/ميلاد/فصيلة/حساسية/جهات/أدوية/QR مرسوم) → إعادة بناء كاملة من /users/me/profile + /health/emergency-contacts + /health/chronic-meds مع QR حقيقي (react-native-qrcode-svg) يشفّر الهوية الوطنية.
- ماسح الباركود: زر «محاكاة المسح» → كاميرا حقيقية (expo-camera) + بحث /medicines/by-barcode الحقيقي مع حالة «غير موجود».
- تحليل الوجبة بالصورة: كان يحلل نصًا ثابتًا («كبسة لحم») بدل الصورة → expo-image-picker + image_base64 للمسار البصري الحقيقي.
- SessionManager: كان يختلق توكنات 'new-access-token' → تدوير حقيقي عبر /auth/refresh مع إبطال عند الرفض.
- LocationService.calculateDistance كان يرجع 0 (يجعل isWithinServiceRadius صحيحًا دائمًا) → haversine حقيقي.
- حذف شاشة live-doctor-tracking (محاكاة كاملة: ETA متناقص وطبيب وهمي «د. أحمد») + حذف 11 ملف module placeholder ميتًا + خريطة تشخيصات مرسومة بنسب ثابتة + AuthAuditLogger IP_PLACEHOLDER.
- chronic-disease/trends: عرض شرطي صادق للحقول الناقصة + حالة فارغة + حارس normal.
**العدّادات الخام بعد R5:** مريض 191 (انخفاض من 219 — الباقي خصائص placeholder قياسية وتعليقات حميدة موثقة)، مزود 318، أدمن 55، باك 28 (كلها حميدة). المواضع المشبوهة الحقيقية: **صفر في المريض والمزود والباك إند**؛ الأدمن لم تبدأ مراجعته بعد.

## Run بعد R6 (commit 9e8386c) — رحلة التأمين + الماليات
**ما تغيّر (باك إند):**
- insurance: OCR بطاقة التأمين كان يُرجع بيانات مختلقة (bupa/Ahmed Obaid/هوية وهمية) → استخراج حقيقي بالذكاء البصري عبر AiGateway مع رفض صادق عند الفشل؛ أهلية NPHIES كانت تُرجع eligible:true دائمًا مع معرّف معاملة مختلق → فحص صادق من الوثيقة المخزنة مع nphies_live:false صراحة؛ uploadPolicy كان يختلق بوليصة tawuniya → حفظ حقيقي غير موثّق؛ submitClaim بلا افتراضات مختلقة (100/80).
- wallet: getSpendingData كان ثابتًا (350/800/200 مع كائن مكسور) → تجميع حقيقي من معاملات المحفظة؛ نسبة العمولة 15% الثابتة وحد التعليق 500 → من finance_config؛ addCard بلا اختلاق (0000/12/30) — تحقق إلزامي؛ تعليق escrow المضلل صُحّح.
**ما تغيّر (مريض):**
- add-policy: كان يرسل 'base64_simulated_data' ويحفظ verified:true بعضوية مزيفة («أحمد عبيد») → كاميرا حقيقية + OCR حقيقي + حفظ غير موثّق بالبيانات المُدخلة فقط؛ شبكة/فئة/انتهاء لم تعد مختلقة.
- approval-pending: كان يوافق تلقائيًا بعد 3 ثوانٍ بتحمل 20% و350 ريالًا → استطلاع /insurance/requests الحقيقي كل 6 ثوانٍ وعرض التحمل الفعلي فقط.
- network-providers: كان يستخدم مصفوفة PROVIDERS غير معرّفة (انهيار عند التشغيل!) → /providers بمرشحات شركة/شبكة/فئة التأمين الحقيقية.
- policy-detail وrefund-status: بيانات ثابتة → /users/me/profile و/refunds/my الحقيقيان.
**المواضع المشبوهة الحقيقية المتبقية:** صفر في المريض/المزود/الباك إند/الأدمن (الأرقام الخام = خصائص placeholder قياسية وتعليقات حميدة وملفات أرشيف source_files).

## Run بعد R7 (commit d6a8731) — الإشعارات الحقيقية + ترجمة المفاتيح
**ما تغيّر (باك إند):**
- notifications.service.listForUser: كانت تُرجع title_key/body_key الخام (مثل 'notification.appointment.created.title') فيظهر المفتاح للمستخدم بدل النص → الآن تُحلَّل عبر I18nService بلغة المستخدم مع params، وتُحسب راية read من read_by خادميًا.
- i18n DICTIONARY: نطاق notification.* (5 مفاتيح × عنوان/نص، عربي/إنجليزي/أردو) كان غائبًا رغم أن منشئي الإشعارات يستخدمونه → أُضيف. النطاقات notif.* (112) وpharmacy.* (23) كانت موجودة مسبقًا؛ العناوين العربية الخام تمرّ كما هي بسلام.
**ما تغيّر (مريض):**
- app/notifications/index.tsx إعادة بناء كاملة: كانت 8 إشعارات مختلقة (تذكير موعد وهمي، جرعة ميتفورمين، برومو NABDAH25، نتائج تحاليل، عرض كاشباك 10%) → جلب حقيقي من GET /notifications مع سحب للتحديث، تنسيق زمن نسبي، خريطة type→مجموعة/أيقونة/لون، حالات فارغة وخطأ/إعادة محاولة صادقة؛ «قراءة الكل» موصول بـ POST /notifications/read-all، والضغط على إشعار يسجّل القراءة وينتقل عبر action.route.
**العدّادات الخام بعد R7:** مريض 193، مزود 318، أدمن 55، باك 28 — دون تغيير (كلها حميدة موثقة: خصائص placeholder قياسية في RN وتعليقات وأرشيف). المواضع المشبوهة الحقيقية المتبقية: **صفر في المريض/المزود/الباك إند/الأدمن**.

## Run بعد R8 (commit 84987e4) — وحدة العائلة
**ما تغيّر (باك إند):**
- مسارا GET/POST ‏/family/chat/messages جديدان (مجموعة family_messages مربوطة بالمجموعة العائلية مع حل أسماء الأعضاء) — المحادثة العائلية لم يكن لها وجود حقيقي.
- getEmergencyContacts أُغني بأرقام هواتف الأعضاء من users (كانت ترجع أسماء فقط بلا إمكانية اتصال).
- respondPermission يقبل الآن مجموعة صلاحيات جزئية — الموافقة الجزئية حقيقية ولا تتجاوز المطلوب أبدًا.
**ما تغيّر (مريض):**
- family/chat: كانت تجلب مسارًا غير موجود وترسل محليًا فقط مع واجهة صوتية وهمية وعدّاد «3 أفراد» ثابت → جلب وإرسال حقيقيان مع استطلاع كل 5 ثوانٍ وتمييز رسائلي من ملفي وحالات فارغة صادقة.
- family/emergency-contacts: كانت 3 جهات مختلقة بأزرار ميتة → بيانات حقيقية مع اتصال tel: ودعوة وحالات فارغة.
- حذف family/voice-call (محاكاة كاملة: مؤقّت وهمي ومتصل مختلق وأزرار كتم ميتة) وكل مراجعها.
- إصلاح تعاقد معطّل: hub كانت تمرّر memberId بينما الشاشات تقرأ id/name/relation — member-health كانت تعرض «فاطمة أحمد» المختلقة دائمًا وpermissions كانت سترسل هدفًا فارغًا.
- family/calendar: أُعيد بناؤها — أزيلت رقائق أيام بأرقام مختلقة (18+i) لا ترشّح شيئًا، وإدراجات/حذف محلية تتظاهر بالحفظ، وأوقات أحداث مختلقة («غداً 11:00 ص»)؛ shared-calendar صارت تحويلة إليها.
- invite: كود 'NABDAH-ERROR' الوهمي → حالة خطأ/إعادة صادقة؛ شبكة QR مرسومة مزيفة → QR حقيقي لرابط الدعوة.
- permission-request: مفاتيح تبديل الصلاحيات كانت تجميلية لا تؤثر → تُرسل للباك إند (موافقة جزئية)، والفشل لم يعد يعرض نجاحًا زائفًا.
- أيقونات مكسورة (أسماء غير صالحة في MaterialCommunityIcons): testTube/calendarCheck/copy/qrScan.
**المواضع المشبوهة الحقيقية المتبقية:** صفر في المريض/المزود/الباك إند/الأدمن.

## Run بعد R9 (commit 7910065) — تدقيق الأمان
**الثغرات المُغلقة:**
- doctor-referrals كان بلا أي مصادقة: أي شخص كان يستطيع قراءة إحالات أي طبيب (أسماء مرضى + نتائج تشخيصية) أو إصدار روشتات باسم أي طبيب (IDOR كامل) → حارس JWT على الكنترولر + تحقق ملكية (المعرف المطلوب يطابق uuid المستخدم أو _id المحلول، مع استثناء أدمن).
- مفاتيح JWT الاحتياطية الضعيفة: chat.gateway كان يتحقق بسر علني ثابت عند غياب JWT_SECRET، وprovider.module كان يوقّع بـ 'change-me' → كلاهما يفشل بإغلاق في الإنتاج ويشاركان سر التطوير الموحد (auth.guard أيضًا).
- فهرس boot-time لمجموعة family_messages الجديدة (group_id + created_at).
**تأكيدات:** 64 كنترولر: 61 محروس، اثنان عامّان بحق (مجسّات system-health، وويب هوك المدفوعات الموقّع). موجود ويعمل: helmet، قائمة CORS بيضاء، مُقيّد معدل عام (200/دقيقة)، OTP في Redis بمهلة وحد 5 محاولات، bcrypt(12) لكلمات المرور.

## Run بعد R10 (commit 44c00b7) — LiveKit + إلغاء ازدواجية المحادثة + تحققات البنية
**ما تغيّر:**
- LiveKit: التطبيقات تشير إلى wss://livekit.nabd.plus والنطاق غير موجود (NXDOMAIN) — مكالمات الفيديو كانت ستفشل كلياً. النقطة الصحيحة wss://live.nabd.plus (تعمل عبر Cloudflare، HTTP 200). صُحّحت في 5 ملفات (مريض env + شاشتان، مزود fallback + env.example).
- محادثة العائلة: تبيّن أن وحدة compat تخدم /family/chat/messages أصلاً (مجموعة familychatmessages، مغلّف {data}) — أُلغيت المسارات المكررة التي أضافها R8 في family.controller وبقيت الشاشة المعاد بناؤها بعد محاذاتها للمغلّف.
**تحققات حية (بلا وصول SSH):**
- الإنتاج يعمل بنسخة قديمة: /health/emergency-contacts POST و/emergency/sos/cancel يرجعان 404 (مسارات R4/R5 غير منشورة بعد) — النشر معلّق فقط على مفتاح nabd_deploy_key المفقود من هذه البيئة.
- FCM: google-services للتطبيقين متطابقان ومشروع واحد مسجّل فيه الحزمتان؛ الباك إند متعدد المشاريع عبر FCM_PROJECT_ID(_2) — قيم المفاتيح على السيرفر تُفحص بعد SSH.
- R2 CDN: حي (pub-8fac6a8c9296b585e3a5f71a1a2baa89.r2.dev — 401 على الجذر طبيعي).
- الأدمن الحي: admin.nabd.plus يخدم Next.js (web-admin) بإثبات __NEXT_DATA__ — مجلد frontend/ القديم (CRA) لم يُحذف شيء منه؛ كل شاشاته الأربع لها مكافئ في web-admin (audit-logs، nursing-portal…).

## Run بعد R11 (commit e349410) — النشر الإنتاجي الفعلي + إصلاحات البناء
**النشر:** تم نشر الباك إند المعدّل فعلياً على السيرفر (OVH 57.131.133.208:/opt/nabdah) عبر SSH بالمفتاح المرفوع؛ نسخة احتياطية من src قبل المزامنة (backups/src-pre-r10-*.tar.gz) ثم rsync ثم إعادة بناء صورة backend وتشغيلها — الحاوية healthy وكل الحاويات التسع تعمل.
**إصلاحات البناء الصارم (nest build/tsc على السيرفر أصرم من فحوص transpile المحلية — 4 تكرارات حتى الأخضر):**
- enums: إضافة EmergencyState.CANCELLED (من R4) كسرت اكتمال Record في EMERGENCY_TRANSITIONS → أُضيفت الانتقالات (CANCELLED هدف من كل الحالات النشطة، ومن CANCELLED إلى CLOSED فقط).
- radiology.controller: 8 توابع مكررة (تعارض TS2393) أزيلت — كتلة R3 كررت مسارات موجودة.
- TS2742 في facility-ops/home-care/health: توصيفات Promise<any[]> صريحة وقوالب as any عند $push.
- tsconfig.build.json جديد يستثني spec/scripts من بناء الإنتاج + nest-cli tsConfigPath؛ Dockerfile.backend على السيرفر كان لا ينسخه (sed fix موثق) — يجب إبقاء هذا السطر في أي إعادة بناء.
- insurance.service.spec أُعيدت كتابتها لسلوك R6b الصادق (AiGateway mock، eligible:false بدون وثيقة)؛ analytics.service.spec حُذف (كان يختبر خدمة محذوفة).
**إصلاحات وظيفية حرجة اكتشفها النشر:**
- DoctorReferralsController لم يكن مسجلاً في أي Module إطلاقاً — واجهة إحالات المزود (inbox/issue) كانت 404 منذ الأبد! سُجّل في care.module مع مخططي EncounterReferral وDoctorProfileExtended.
- notifications FCM: الكود يقرأ FIREBASE_* فقط بينما السيرفر يضبط FCM_* → fallback ثنائي الاتجاه (FIREBASE_* || FCM_*) — الإشعارات الأصلية كانت معطّلة فعلياً على الإنتاج.
- Moyasar refund: fallback 'sk_test_dummy' → MOYASAR_SECRET_KEY || MOYASAR_SECRET مع رفض صريح عند الغياب.
**تحقق حي بعد النشر:** liveness 200؛ 401 «Missing token» (الحارس العام يعمل) على: /health/emergency-contacts POST، /emergency/sos/cancel، /notifications/read-all، /facility/announcements، /family/chat/messages، /provider/doctor-referrals/my-referrals/:id — كل المسارات الجديدة منشورة ومحروسة.
**تدقيق متغيرات البيئة (وجود فقط، بلا كشف قيم):** FCM وR2/S3 وCloudinary وLiveKit (wss://live.nabd.plus) وResend وGemini/OpenAI وCORS كلها مضبوطة. **بنود مالك معلّقة:** ZATCA_VAT_NUMBER غير مضبوط (الفوترة الإنتاجية تفشل بإغلاق حسب تصميم R6b)؛ MOYASAR_WEBHOOK_SECRET غير مضبوط (تحقق توقيع الويب هوك يُتخطى — ويب هوك المدفوعات غير موثّق حالياً).

## Run بعد R12–R15 (commits 4768d86 + 6d1502b) — EPIC 1: المحرك المالي الكامل + النشر الإنتاجي
**النطاق:** تدقيق وإصلاح المنظومة المالية End-to-End (20 قسماً من ملف المالك): المدفوعات، الاسترداد، الإلغاء، الإرجاع، المحفظة، الكوبونات، الولاء، العمولات، الحسابات المعلّقة (Escrow)، الرصيد السالب، السندات المالية غير القابلة للتعديل، كشف الاحتيال، التقارير، الفواتير، والموافقات الإدارية (maker-checker).
**المنهج:** 21 ملاحظة (F1–F21) بما فيها 7 حرجة P0 — كلها أُصلحت في الكود الحقيقي بلا افتراضات. التفاصيل الكاملة في تقرير EPIC1 (انظر مخرجات الجلسة).
**أخطر ما أُغلق:**
- شحن المحفظة كان يصكّ مالاً بلا بوابة دفع (F1) → الآن intent عبر Moyasar + تأكيد ذري (pending_payment→credited مرة واحدة).
- إنشاء الدفع كان يثق بالمبلغ القادم من العميل (F2) → المبلغ يُحسب من قاعدة البيانات مع تحقق ملكية.
- إلغاء الطلب لم يكن يسترد المال إطلاقاً (F9 — شرط state=='PAID' مستحيل لأن الـ enum بلا PAID) → مصفوفة إلغاء كاملة تعتمد payment_status.
- تنفيذ السحب لم يكن يسجّل قيد payout (F23 — نفس المال قابل للصرف مرتين) → قيد idempotent + تحقق من الرصيد المتاح.
- شاشة دفع الصيدلية في تطبيق المريض كانت تعرض نجاحاً مزيفاً بعد 1.5 ثانية مهما كانت النتيجة (F4) → تدفق Moyasar حقيقي (intent → صفحة مستضافة → تحقق دوري).
- الإجمالي كان يؤخذ من العميل (F22) → يُحسب في الخادم مع كوبون/نقاط/محفظة.
**النشر (R15):** 6 محاولات بناء فاشلة كشفت سلسلة أعطال متداخلة: npm يصاب بعطل «Exit handler never called» ويخرج برمز 0 (فمرّرت طبقات تالفة)، والسبب الجذري أن lockfile أُعيد توليده محلياً فحمل 13 رابطاً لمرآة داخلية غير قابلة للوصول من الإنتاج — أُصلحت إلى registry.npmjs.org فاكتمل npm ci في 34 ثانية. كما أصلح tsc الصارم 9 أخطاء أنواع (round2 غير معرّف، shorthand points، قوالب casts). البناء النهائي: node_modules + dist في حاويات عادية مع كاش npm دائم، وDockerfile.backend يكتفي بنسخ المخرجات الموثّقة.
**تحقق حي بعد النشر:** الحاوية healthy بلا أخطاء إقلاع؛ 18 مساراً مالياً جديداً كلها 401 «Missing token» (موجودة ومحروسة): coupons/validate، loyalty/redeem-quote، provider/balance، admin/finance-engine (reports/approvals/commission-rules/refunds/fraud/provider-balance)، wallet/topup(+confirm+get)، billing/invoice pdf+email، pharmacy/returns/eligibility، payments/capture، provider/payouts/balance، coupons (admin) — وغير الموجود 404. مكتبة qrcode داخل الصورة (فواتير ZATCA QR).
**بنود مالك معلّقة (لم تتغير):** ZATCA_VAT_NUMBER وMOYASAR_WEBHOOK_SECRET غير مضبوطين على السيرفر.

## Run بعد R16 — EPIC 1: اختبار E2E حي على الإنتاج (28/28) + إصلاحان اكتشفهما الاختبار
**المنهج (S19):** جناح qa-epic1.js (28 فحصاً) يعمل داخل حاوية الإنتاج نفسها: يزرع محفظة 200 ر.س ونقاط ولاء 500، ينفّذ السيناريوهات عبر HTTP الحي (POST /orders/create، /cancel، coupons، loyalty، approvals، payouts، reports)، ويقرأ MongoDB مباشرة للتحقق من الأرصدة والقيود، ثم ينظّف كل بياناته (qaepic1_*). الأدلة: runs/20260802T170421Z_epic1_qa_28of28.log (تشغيلان متتاليان أخضران، exit 0).
**خللان حقيقيان اكتشفهما الاختبار الحي وأُصلحا ونُشرا:**
1. **F24 (P1):** create-order.dto.ts لم يصرّح بـ coupon_code ولا loyalty_points → ValidationPipe (whitelist) كان يرفض كل طلب فيه كوبون/نقاط بـ 400 — أي ميزتا الكوبونات والولاء غير قابلتين للوصول من التطبيق إطلاقاً. الاكتشاف: الجولة الأولى 11/15. الإصلاح: حقلان @IsOptional موثّقان، أعيد البناء والنشر.
2. **F25 (P0):** مخطط Order صارم (strict) وبلا حقول مالية → wallet_applied/coupon_discount/loyalty_points_used/refund_status كانت تُسقط silently عند الحفظ؛ عند الإلغاء قرأ cancel() قيمة wallet_applied=0 فتخطّى إعادة رصيد المحفظة (المريض يخسر المدفوع محفظةً!) وبقي payment_status='paid'. الاكتشاف: الجولة الثانية 26/28. الإصلاح: 12 حقلاً مالياً في order.schema.ts + cancel() يحدّث payment_status='refunded'/refunded_at ويثبّت رسوم الإلغاء في نفس updateOne (كانت تُضبط على نسخة ذاكرة لا تُحفظ). أعيد البناء على السيرفر (npm ci + nest build + prune --legacy-peer-deps، صفر أخطاء tsc) ونُشر بـ --force-recreate.
**النتيجة النهائية:** 28/28 PASS مرتين متتاليتين: إجمالي خادمي 55−10−5=40، خصم محفظة مرة واحدة بالضبط، دورة كوبون كاملة (تحقق→استخدام→تحرير)، دورة نقاط كاملة (اقتباس بسقف 20%→خصم→استرداد)، إلغاء يعيد المحفظة 160→200، maker-checker (اعتماد ذاتي 403)، رصيد سالب −25 وحظر سحب عليه، عمولة من finance_config (11%)، تقارير مفككة، منع إرجاع غير المُسلَّم، شحن محفظة fail-closed.
**بند مالك حرج اكتشفه الاختبار:** Moyasar ترفض كل العمليات الحية برسالة «Entity not activated to use live account» — لا يمكن لأي دفع بطاقة حقيقي أن ينجح حتى يفعّل المالك الكيان في لوحة Moyasar. الكود يفشل بإغلاق (لا رصيد بلا دفع) وهذا صحيح. يضاف إلى البنود المعلّقة: ZATCA_VAT_NUMBER، MOYASAR_WEBHOOK_SECRET.
