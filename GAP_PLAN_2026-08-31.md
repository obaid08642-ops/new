# خطة الفجوات — Nabd Plus — تم إعدادها وتنفيذها جزئيًا في 2026-08-31

الفرع: `chore/prod-hardening` (لم يُلمس `main` — دمج main من اختصاص المراجع)
المرجع: فحص `main` (75e0a34d) مقابل الفرع (يسبق main بـ 23 commit حتى c1a31b02، ثم a47eda74 SEO).

## أ- مثبت ومرفوع (verified SHA على remote)
- c1547384 public-first launch | 396ceca5 pharmacy radius | 222140b8 chunked runner
- 6cc89e51 changelog | a42519ba CI provider/admin | 55c1ee20+241695a4 LiveKit
- 84171d8d wallet | cbc48a41 pharmacy offers | 8d18f291 lab booking | 7ef70756 lab tracking
- c1a31b02 family details/calendar | a47eda74 SEO public metadata (robots/sitemap/4 صفحات) — تُرفع في هذه الجلسة.

## ب- ادعاءات التقرير القديم غير الموجودة في أي فرع (NOT FOUND — غير قابلة للاعتماد)
- 2556a276 (address manager), d92eefff (report sharing), ba8953f2 (docs), 2d9581bc (robots), 73f60315 (metadata)
- docs/PROJECT_MASTER_REPORT_AR.md + COMPETITOR_BENCHMARK + DISCOVERABILITY_AUDIT + RELEASE_BACKLOG
- "Phase 11" في IMPLEMENTATION_PLAN.md
القرار: إما استرجاعها من بيئة الـ Agent السابق، أو يُعاد بناؤها من جديد. لا يُبنى عليها شيء.

## ج- فجوات مؤكدة في الكود (مطلوب تنفيذها على هذا الفرع)
1. Metadata عام لبقية المسارات العامة: تفاصيل articles، تفاصيل doctors/radiology/labs/packages، category pages (موجودة جزئيًا).
2. Typed JSON-LD فقط من بيانات حقيقية: Physician, MedicalClinic/Hospital, Service, MedicalWebPage, BreadcrumbList, MedicalDrug, Product/Offer (بسعر/توفر حقيقي فقط — ممنوع بيانات وهمية).
3. catalog eligibility: approved + public_eligibility + indexing_eligibility + provenance + last_reviewed (الحقول جزئية في backend: approval-workflow / catalog-publication / seo-search) — إغلاق الربط + اختبارات.
4. Internal search contract موحد: medicine + doctor + facility + service + insurer، Arabic/English normalization + synonyms + pagination (أساس seo-search موجود: search/global, public/products/search).
5. archive/noindex/canonical policy للفلاتر والمنتجات القديمة (pagination >1 noindex موجود في category).
6. Core Web Vitals على staging بقياس حقيقي فقط — لا ادعاء بدون قياس.
7. Search Console / Bing / IndexNow — فقط عند توفر credentials و domain access.
8. إعادة بناء (أو استرجاع) المسارات المفقودة المزعومة: share-report, profile/addresses, video-call — لفحصها على main.
9. لا تنفيذ checkout/شراء دواء عبر Agent بدون confirmation + permission + idempotency + audit.

## د- متبقٍ من خطة الإنتاج (من تقرير الحالة — بانتظار التنفيذ)
- Pharmacy staging E2E (3→5→8 km, cumulative offers, partial availability, substitutes, self-pickup 15km, cash, insurance, copay, override audit, notifications, expired round).
- Security matrix (ownership, BOLA, PHI SSR, log redaction, rate limits, session rotation).
- Payments (card/cash/insurance, idempotency, refund, ledger reconciliation).
- Provider onboarding + document review + admin approval/rejection/resubmission.
- Admin RBAC + Passkey + price-override audit UI.
- Production gates (backups, rollback, monitoring, release artifact, sign-off).
- P2: @ts-nocheck removal، design system، accessibility، RTL QA، release builds، load testing.

## هـ- التحقق (قاعدة إلزامية لكل دفعة)
focused tests → suite كاملة → lint (إن وجد) → typecheck → production build → changelog → commit → push → إثبات remote SHA == local SHA.
ملاحظة: في بيئة هذه الجلسة فشل تثبيت التبعيات (npm ERESOLVE ثم exit 254) — لم تُشغَّل الاختبارات، الحالة PENDING حتى CI.

## تحديث ثانٍ 2026-08-31 — تدقيق موثق + P0 منفذ
### تدقيق (أدلة أوامر):
- بيانات وهمية: عدادات غير-اختبارية mock|fake|dummy|lorem|placeholder|hardcod|stub|TODO → patient-web 188 / patient-app 369 / provider-app 438 / admin 224 / backend 560. المراجعة التفصيلية: كل النتائج placeholder حقول إدخال أو تعليقات تؤكد إزالة الوهمي ("never mock data", "no more dummy token fallback"). لا يوجد مصدر بيانات مخترع. تحقق ثابت فقط (بدون build).
- أزرار ميتة/روابط "#": 0 في patient-web/app و patient-app/app و provider-app/src و admin/src.
- Provider: 66 استدعاء شبكة عبر الشاشات؛ الملفات بلا شبكة هي مكونات عرض فقط. build غير مشغل → غير مؤكد نهائياً.
- Parity ويب/موبايل: ويب 90 route، موبايل 240 شاشة؛ فرق بالاسم طويل (فيه مكافئات دلالية). المصفوفة الكاملة مطلوبة كمراجعة شاشة-بشاشة P1.
### P0 المنفذ (هذه الجولة):
- [locale]/profile/addresses (قائمة/إضافة/حذف عبر users/me/addresses مع requirePatientAccess)
- [locale]/consultations/share-report (قائمة تقارير حقيقية /medical-reports/mine + مشاركة روابط آمنة)
- lib/api/addresses-server.ts, reports-server.ts, components-next/addresses.tsx, share-report.tsx, i18n للغات الست
- الحالة: مرتفع في هذه الجولة؛ اختبار/typecheck/build بانتظار CI.
