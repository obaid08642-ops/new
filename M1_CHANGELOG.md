# سجل تغييرات المرحلة M1 — التوصيل الفعلي لتطبيق المريض

**التاريخ:** 23 يوليو 2026 · **الحالة:** مكتملة ومُتحقق منها ✅
**إثبات الجودة:** `tsc --noEmit` لتطبيق المريض كاملًا (488 ملفًا) = **صفر أخطاء** · الباك إند بعد إضافاته = صفر أخطاء

---

## التحول الجوهري

> **قبل M1:** تطبيق المريض كان "عرضًا تجريبيًا" — 157 شاشة (66%) تستدعي دالة `apiFetch` وهمية لا تلمس الشبكة إطلاقًا (`setTimeout(800ms)` ثم بيانات ثابتة أو `{}`)، بما فيها شاشة تسجيل الدخول.
>
> **بعد M1:** نفس الـ 157 شاشة أصبحت متصلة بالباك إند الحقيقي — بلا استثناء، وبإعادة كتابة ملف واحد فقط مع الحفاظ على توقيع الدالة.

## التعديلات التفصيلية

### 1️⃣ M1-01 — إنهاء عصر الـ Mock (الملف المحوري)

**`utils/api.ts` — أُعيد كتابته بالكامل:**
- **كان:** دالة وهمية لا تتصل بالشبكة، بيانات ثابتة لـ 5 مسارات فقط، و`{}` لكل الباقي.
- **أصبح:** عميل شبكي حقيقي فوق `HttpClient` (axios) يرث منه Retry بـ Exponential Backoff + Offline Queue للطفرات، ويضيف:
  - حقن تلقائي لـ JWT من التخزين الآمن (`SecureStore` ثم `AsyncStorage` كبديل).
  - تحليل `body` سواء كان نصًا (الشاشات تستخدم `JSON.stringify`) أو كائنًا.
  - تطبيع الأخطاء برسائل عربية واضحة من رد الخادم (401/404/429/5xx/انقطاع الشبكة) — **بلا ابتلاع صامت**.
  - مسح الجلسة تلقائيًا عند 401 ليعود التطبيق لتدفق الدخول.
  - دالة `storeAuthSession()` جديدة تحفظ `accessToken + refreshToken` بشكل موحد.

### 2️⃣ M1-ENV — عناوين البيئات بدل الثوابت الحرفية

| الملف | كان | أصبح |
|---|---|---|
| `src/services/HttpClient.ts` | `baseURL` ثابت حرفيًا | `EXPO_PUBLIC_API_URL` + `/api/v1` — يكفي متغير واحد للتبديل بين dev/staging/prod |
| `src/constants/index.ts` | `.../v1` (**خاطئ — بلا `/api`**) | يُشتق من نفس المتغير بالبادئة الصحيحة |
| `FASTAPI_BASE_URL`, `CDN_URL` | ثابتة | متغيرات بيئة `EXPO_PUBLIC_AI_URL` / `EXPO_PUBLIC_CDN_URL` |

> للتجربة محليًا: ضع في `.env` سطرًا واحدًا `EXPO_PUBLIC_API_URL=http://192.168.1.X:8002` (IP جهازك) وسيتصل التطبيق بالباك إند المحلي فورًا.

### 3️⃣ M1-10 — إصلاح تدفق الدخول كاملًا

| الشاشة | الإصلاح |
|---|---|
| `(auth)/login.tsx` | استخراج `token.accessToken` (الباك إند يعيد كائنًا، والشاشة كانت تخزّن الكائن نفسه كأنه نص!) + حفظ الـ refresh token عبر `storeAuthSession` |
| `(auth)/login.tsx` + `register.tsx` (الدخول الاجتماعي) | **حذف `|| 'dummy_jwt_token'`** — لا جلسات مزيفة بعد اليوم؛ إما جلسة حقيقية أو خطأ صريح |
| `(auth)/otp.tsx` | الباك إند يعيد `{ok:true}` والشاشة كانت تنتظر `verified` (فكان التسجيل يتعطل دائمًا!) — تقبل الآن الاثنين + استخراج accessToken الصحيح من register/convert-guest |

### 4️⃣ حسم العقود الثلاثة المكسورة (M1-30/31/32)

| العقد | كان (مكسور) | أصبح (حقيقي) |
|---|---|---|
| **الفيديو** `video-call.tsx` | `GET /care/appointments/:id/video-token` (غير موجود) ثم **رمز مزيف `"session_token"`** | `POST /calls/initiate` ← `POST /calls/:sessionId/join` (LiveKit الحقيقي) — وخطأ صريح إن لم يصل رمز |
| **الطوارئ** `sos-active.tsx` | `GET /emergency/sos/status` (غير موجود — وكل مسارات الطوارئ كانت للأدمن فقط!) | مسار **جديد في الباك إند** `GET /emergency/my/active` يرجع استغاثة المريض الحالية فقط |
| **محادثة الطبيب** | `/chat/history/:id` + `/chat/send` (غير موجودة) | `POST /chat/threads/direct` (إنشاء/جلب سلسلة) ← `GET/POST /chat/threads/:id/messages` |
| **محادثة الصيدلي** | `/chat/history?orderId=` (غير موجودة) | `POST /chat/threads/booking` ← رسائل السلسلة الحقيقية |

### 5️⃣ M1-33 — إصلاح الروابط المكسورة

| من | كان | أصبح |
|---|---|---|
| `health/actionable-order.tsx` | `router.push('/labs')` — مسار غير موجود | `/diagnostics/search` |
| `search/index.tsx` | `/pharmacy/product/${id}` — الشاشة اسمها مختلف | `/pharmacy/product-detail` مع params |

### 6️⃣ M1-05 — مكوّن حالات الشاشة الموحّد (جديد)

**`src/components/ScreenStates.tsx`:** `loading / error / empty` موحّدة بألوان الثيم الفعلية وأيقوناته، مع زر "إعادة المحاولة" — جاهز للتعميم على الشاشات الـ 239 (17% فقط كانت تعالج الحالة الفارغة). التعميم الكامل يستمر ضمن الدفعات القادمة.

### 7️⃣ الباك إند — إضافة واحدة

| الملف | الإضافة |
|---|---|
| `emergency/emergency.controller.ts` + `.service.ts` | `GET /emergency/my/active` — أول مسار طوارئ للمريض (كل المسارات كانت `ADMIN` فقط) |

---

## نتائج التحقق

| الفحص | النتيجة |
|---|---|
| مراجع لدوال الـ Mock القديمة (`setTimeout` الوهمي) | **صفر** — الملف أُعيد كتابته كاملًا |
| `tsc --noEmit` تطبيق المريض (488 ملفًا) | ✅ **صفر أخطاء** |
| `tsc --noEmit` الباك إند بعد إضافة الطوارئ | ✅ **صفر أخطاء** |
| عبارات `'dummy_jwt_token'` / `'session_token'` المزيفة | ✅ **حُذفت كلها** |
| عناوين API ثابتة حرفيًا | ✅ **صفر** — كلها متغيرات بيئة |

## المتبقي من M1 (يُستكمل في الدفعة القادمة)
- تعميم `<ScreenState>` على بقية الشاشات (البنية جاهزة).
- ربط مسارات تحتاج endpoints جديدة في الباك إند (سلة `/cart`، `/home-care/services`...) — مجدولة في M2-01/02.
- اختبار E2E حي: دخول ← حجز ← فيديو على خادم staging.

**التالي: M2 — سدّ فجوات الـ APIs الحرجة للمزود والمريض في الباك إند + استكمال ربط التدفقات.**
