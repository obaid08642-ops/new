# حزمة إصلاح تدقيق منصة نبض

هذه الحزمة تحفظ **الملفات المعدلة فقط** من مساحة إصلاح معزولة، وليست نسخة تشغيل أو نشر. الغرض منها مراجعة التغييرات أو نقلها إلى المستودعات الأصلية عبر مراجعة رمز منظمة. لا تحتوي الحزمة على `node_modules` أو مجلدات البناء أو بيانات اعتماد أو حسابات sandbox.

| الدليل | المحتوى | حالة التحقق |
|---|---|---|
| `backend-fixes/` | إصلاحات الأمن والمصادقة وتوجيه API والرعاية المنزلية | `npm run build` ناجح |
| `provider-app-fixes/` | عقد دخول المزود، شاشات التسجيل، إعداد Jest واختبارات التدفقات | 3/3 اختبارات ناجحة |
| `patient-app-fixes/` | إشعارات، QR موقّع، شاشة أوامر طبية ومسار API متوافق | تصدير iOS ناجح |
| `admin-app-fixes/` | عميل API، تدفقات التمريض، إزالة حالات طوارئ تجريبية، CRACO | بناء إنتاجي ناجح |

> يجب دمج هذه الملفات في الشجرة المصدرية المقابلة فقط بعد مراجعة فرق الشفرة وتشغيل اختبارات التكامل على بيئة staging. لا يغير هذا الفرع `main` ولا ينشر أي تغيير.

يرافق هذه الحزمة التقرير النهائي في `audit-artifacts/e2e-traceability-20260814/NABDAH_FINAL_REMEDIATION_REPORT.md`.
