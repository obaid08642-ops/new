# حزمة إصلاح تدقيق منصة نبض

يحفظ هذا الفرع أربع شجرات مصدر كاملة من مساحة إصلاح معزولة، مع الإصلاحات المطبقة عليها. لا يمثل الفرع نشرًا أو بيئة staging، لكنه قابل لبناء المكونات وفق ملفات الاعتماديات المرفقة. لا تحتوي الشجرات على `node_modules` أو مجلدات البناء أو ملفات `.env` الفعلية أو بيانات اعتماد أو حسابات sandbox.

| الدليل | المحتوى | حالة التحقق |
|---|---|---|
| `backend/` | الخلفية الكاملة، وتشمل إصلاحات الأمن والمصادقة والتوجيه والرعاية المنزلية | `npm run build` ناجح في النسخة المعزولة |
| `provider-app/` | تطبيق مزود الخدمة الكامل، يشمل عقد الدخول وشاشات التسجيل وإعداد Jest | 3/3 اختبارات تدفقات ناجحة |
| `patient-app/` | تطبيق المريض الكامل، يشمل الإشعارات وQR الموقع وشاشة الأوامر ومسار API | تصدير iOS ناجح في النسخة المعزولة |
| `admin-app/` | لوحة الإدارة الكاملة، وتشمل الواجهتين القديمة والحديثة وإصلاح CRACO والتمريض | بناء CRA الإنتاجي ناجح في النسخة المعزولة |

> يُبنى كل دليل في بيئة نظيفة باستخدام ملف الاعتماديات الخاص به ومتغيرات staging فقط. لا يغير هذا الفرع `main` ولا ينشر أي تغيير. يلزم نشر staging وتشغيل اختبار E2E قبل اعتماد أي دمج أو إصدار.

يرافق هذه الحزمة التقرير النهائي في `audit-artifacts/e2e-traceability-20260814/NABDAH_FINAL_REMEDIATION_REPORT.md`.
