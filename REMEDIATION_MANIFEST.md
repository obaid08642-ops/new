# حزمة إصلاح تدقيق منصة نبض

يحفظ هذا الفرع أربع شجرات مصدر كاملة من مساحة إصلاح معزولة، مع الإصلاحات المطبقة عليها. لا يمثل الفرع نشرًا أو بيئة staging، لكنه قابل لبناء المكونات وفق ملفات الاعتماديات المرفقة. لا تحتوي الشجرات على `node_modules` أو مجلدات البناء أو ملفات `.env` الفعلية أو بيانات اعتماد أو حسابات sandbox.

> لا يمثل نجاح البوابات المحلية تصريحاً للنشر. يلزم نشر هذا الفرع على بيئة staging معزولة وتنفيذ E2E محكوم بالأدوار والملكية، بما فيه تدفقات الطب والمال، قبل الإنتاج أو المتاجر.

| الدليل | المحتوى | حالة التحقق |
|---|---|---|
| `backend/` | الخلفية الكاملة، وتشمل إصلاحات الأمن والمصادقة والتوجيه والرعاية والصيدلية والطوارئ والأشعة والدفع والإشعارات | `npm run build` ناجح بعد الدفعة الأخيرة |
| `provider-app/` | تطبيق مزود الخدمة الكامل، بما في ذلك إزالة التدفقات الثابتة من الصيدلية والطبيب وتهيئة push الوهمية | 3/3 اختبارات Jest ناجحة بعد الدفعة الأخيرة |
| `patient-app/` | تطبيق المريض الكامل، يشمل الجلسة والطلبات والباركود والطوارئ والتتبع ومسار API ودفع الصيدلية الآمن | تصدير iOS ناجح بعد الدفعة الأخيرة |
| `admin-app/` | لوحة الإدارة الكاملة، وتشمل الواجهتين القديمة والحديثة وإصلاح CRACO والتمريض | بناء CRA وNext.js الإنتاجيان ناجحان بعد الدفعة الأخيرة |

فحصت أسطر الإضافة قبل الالتزامات بحثاً عن مؤشرات الأسرار عالية الثقة ولم يظهر مفتاح أو رمز اعتماد مضاف. العمليات التي لا تملك عقداً خلفياً مخزناً، مثل مراجعة سلة الصيدلية وتفويض التأمين وتعيين التوصيل، تعرض الآن حالة عدم إتاحة صريحة ولا تدعي نجاحاً محلياً.

يبقى `expo lint` لتطبيق المريض محجوباً بتعارض تاريخي في سلسلة ESLint/AJV، ويجب علاجه قبل بوابة الإصدار. لا يغير هذا الفرع `main` ولا ينشر أي تغيير. يلزم نشر staging وتشغيل اختبار E2E قبل اعتماد أي دمج أو إصدار.

يرافق هذه الحزمة التقرير النهائي في `audit-artifacts/e2e-traceability-20260814/NABDAH_FINAL_REMEDIATION_REPORT.md` وخطة الاختبارات اللاحقة في `audit-artifacts/e2e-traceability-20260814/POST_REMEDIATION_E2E_EXECUTION_PLAN.md` وسجل قبول أمر المعالجة في `audit-artifacts/e2e-traceability-20260814/NABDAH_EXECUTION_ORDER_ACCEPTANCE_REGISTER.md`.

ولمراجعة أثر أي عنصر ثابت أو وهمي دون اختصار، يحتوي المجلد نفسه على `NABDAH_DETAILED_RUNTIME_DATA_AND_REMEDIATION_REGISTER.md`. يتضمن هذا الملف نفسه الملحق الآلي الكامل بالملف والسطر لكل مرشح تشغيلي، بعد تنقيح المقتطفات من بيانات الاعتماد وبيانات الاختبار الحساسة.
