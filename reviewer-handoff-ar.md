# مذكرة تسليم للمراجع — تطبيق مزوّدي الخدمات

**الفرع المطلوب مراجعته:** `remediation/provider-production-governed`  
**المرجع الأساسي للمقارنة:** `main`  
**حالة النشر:** **NO-GO حتى استكمال أدلة التكامل الحي والترحيل**

## رسالة مختصرة جاهزة للإرسال

> مرحباً،
>
> أرجو مراجعة فرع `remediation/provider-production-governed` مقارنةً بـ`main`. التغييرات محفوظة في الأرشيفين المتتبعين `NabdProvider-provider.zip` و`nabdah-backend.zip`، ويوجد تقرير تفصيلي في `provider-production-delivery-report-ar.md`.
>
> الهدف هو إزالة بيانات التشغيل الوهمية في تدفقات المزوّد وربط الشاشات بخدمات خادم فعلية. اجتازت التغييرات محلياً بناء الخادم، وفحص TypeScript للتطبيق، وعقود التطبيق، وبوابة بيانات وقت التشغيل، ومجموعة اختبار الخادم الكاملة (90 مجموعة / 483 اختباراً).
>
> **يرجى عدم دمج الفرع أو نشره مباشرةً.** نحتاج أولاً مراجعة الكود، وتشغيل سيناريوهات E2E المحمولة على بيئة مرحلية معزولة، والتحقق من ترحيل موظفي المستشفى بوضع المعاينة قبل تطبيقه، ثم قبول يدوي على أجهزة فعلية. الرجاء تسجيل أي ملاحظات أو أخطاء مانعة في طلب المراجعة.
>
> شكراً.

## ما يرسله صاحب التسليم

أرسل **الرابط إلى الفرع أو طلب مراجعة/دمج** أولاً، لا الأرشيفات وحدها. المراجع يحتاج تاريخ الالتزامات والسياق لكي يقارن بالمرجع الحاكم. أرفق التقرير التفصيلي، ويمكن إرفاق الأرشيفين فقط إذا لم يملك المراجع وصولاً إلى المستودع.

| العنصر | الأولوية | سبب الإرسال |
|---|---:|---|
| رابط فرع/طلب مراجعة لـ`remediation/provider-production-governed` | إلزامي | يتيح مراجعة الالتزامات ومقارنة `main` والتحقق من عدم دمج الفرع الحجري بالكامل. |
| `provider-production-delivery-report-ar.md` | إلزامي | يوضح ما نُفذ، الاختبارات، والـNO-GO وخطوات النشر. |
| `reviewer-handoff-ar.md` | إلزامي | يحتوي رسالة التسليم وقائمة تدقيق المراجع. |
| `NabdProvider-provider.zip` و`nabdah-backend.zip` | عند الحاجة | لأن مصادر `main` محفوظة كأرشيفات؛ لا تغني عن مراجعة الفرع. |
| سجل نتائج E2E المرحلي | شرط قبل الاعتماد | لا يوجد حالياً؛ ينشأ فقط بعد تشغيل بيئة مرحلية معزولة. |

## نطاق المراجعة الفعلي

لم تتم إضافة مجموعة صفحات مستقلة جديدة من الصفر كملفات شاشة جديدة. التنفيذ ركز على **استكمال وربط الشاشات الموجودة** وإزالة ما كان فيها من إجراءات وهمية. بالتالي:

| القياس | العدد | التوضيح |
|---|---:|---|
| ملفات شاشة مستقلة جديدة أُنشئت من الصفر | 0 | لم تكن الحاجة تصميم صفحات جديدة، بل إكمال الشاشات الموجودة وربطها بخدمات حقيقية. |
| مسارات/شاشات إسعاف فُعلت في التطبيق | 2 | تم إدراج Dashboard والتسجيل الموجودين في `App.tsx` ضمن التنقل الفعلي؛ لم يُنشأ ملفان جديدان. |
| تدفقات قطاعات رئيسية عولجت | 7 | الطبيب، الصيدلية، المختبر، الأشعة، التمريض، المنشأة/المستشفى، والإسعاف. |
| مكوّنات واجهة مشتركة عولجت | 5 على الأقل | شبكة الإحالات، العروض، إعدادات الموقع العام، CRM، وإعدادات التوفر. |

> لا تستخدم عبارة «أضيفت X شاشة جديدة» في التسليم؛ العبارة الدقيقة هي: **«تم استكمال وربط شاشات قائمة وتفعيل مسارات الإسعاف الموجودة، وليس إنشاء عدد كبير من ملفات الشاشات الجديدة.»**

## الاختبارات التي تحققت محلياً

| الدليل | النتيجة |
|---|---|
| `packages/shared-contracts`: فحص النوع، البناء، والاختبار | ناجح؛ 7 اختبارات. |
| فحص TypeScript لتطبيق المزوّد | ناجح. |
| عقد إصدار التطبيق | ناجح؛ 10 اختبارات. |
| اختبارات المختبر والأشعة | ناجحة؛ 4 مجموعات و24 اختباراً. |
| اختبارات وحدة `provider-production` | ناجحة؛ 3 مجموعات و9 اختبارات مركزة. |
| بناء خادم NestJS | ناجح. |
| مجموعة الخادم الكاملة | ناجحة؛ 90 مجموعة و483 اختباراً. |
| بوابة بيانات وقت التشغيل | `RUNTIME_DATA_GATE=PASS`. |
| فحص قابلية نقل E2E | ناجح نحوياً وبفحص الرفض المسبق فقط؛ ليس تشغيل E2E حياً. |

## قائمة تدقيق المراجع

يراجع المراجع كل نقطة أدناه قبل الموافقة المشروطة، ثم يقرر عدم الجاهزية أو ينفذ مرحلة البيئة المرحلية.

| المجال | المطلوب من المراجع | معيار القبول |
|---|---|---|
| مصدر التغيير | مقارنة الفرع بـ`main` فقط، وتأكيد أن المصدر الحجري لم يُدمج كاملاً. | التغييرات مقصودة ومحصورة في الأرشيفين والحزم/السكربتات والتوثيق. |
| واجهات العميل | تدقيق أن الطبيب والصيدلية والمختبر والأشعة والمنشأة والتمريض والإسعاف لا تستخدم بيانات تشغيل وهمية في التدفقات المعالجة. | فشل الخادم يظهر للمستخدم ولا يُحوّل إلى نجاح أو سجلات وهمية. |
| الإحالات | تدقيق `createReferral` وشبكة الإحالات. | تحقق من المريض، وربط وملكية الموعد عند تقديمه، ومن اعتماد الوجهة عند تقديم معرفها. |
| العروض | مراجعة مسار العروض قبل أي تحصيل. | لا تتحول إلى دفع/سعر نهائي إلا بعد قرار مراجعة إدارية أو نموذج تسعير خادمي معتمد. |
| التخزين والمكالمات | تدقيق تكامل الأشعة والمختبر وLiveKit. | لا اعتماد قبل اختبار رفع/استرجاع ملفات حقيقي ومكالمة حية بصلاحيات صحيحة. |
| الترحيل | تشغيل ترحيل موظفي المستشفى بوضع المعاينة أولاً. | نسخة احتياطية، عدادات مطابقة، ثم تطبيق صريح ومراجعة لاحقة. |
| E2E | تشغيل سيناريوهات `backend/e2e` على بيئة قابلة للحذف. | تعيين `BASE`, `MONGO_URI`, `E2E_DB`, `E2E_JWT_SECRET`, `E2E_ALLOW_DESTRUCTIVE=true`، والاحتفاظ بالمخرجات. |
| قبول الأجهزة | اختبار Android وiOS فعلياً. | Socket.IO وCamera وDocument Picker والخرائط والإشعارات تعمل في التدفقات السبعة. |

## قرار النشر الصحيح

لا يرسل المراجع موافقة نشر الآن. القرار الصحيح هو **مراجعة كود ثم موافقة مشروطة** فقط، إلى أن تكتمل عناصر الـNO-GO التالية: تشغيل E2E على بيئة مرحلية، ترحيل المستشفى بعد النسخ الاحتياطي، اختبار S3/R2 وLiveKit وOTP والإشعارات، واختبار الأجهزة. بعد توثيق هذه الأدلة يمكن للمراجع اعتماد الدمج إلى `main` ثم تنفيذ النشر وفق إجراءات الفريق.
