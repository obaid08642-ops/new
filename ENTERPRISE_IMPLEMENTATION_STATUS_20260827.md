# حالة تنفيذ مركز التحكم المؤسسي

**التاريخ:** 27 أغسطس 2026

**فرع التنفيذ:** `agent/enterprise-control-center-20260827`

**المرجع الوظيفي:** ملف المتطلبات المرفق، ومصدر `main` المفكوك، والتغييرات المنتقاة من `quarantine/workstation-source-51a84c7`.

## قرار الدمج والمصدر

لم يُنفذ دمج Git مباشر مع `quarantine/workstation-source-51a84c7`، لأن تاريخه مستقل عن `main` ولأن فرع `main` كان يحتفظ بحزم المصدر داخل أرشيفات. بدلاً من ذلك، فُكت مصادر `main` في شجرة قابلة للبناء على فرع تنفيذ مستقل، ثم نُقلت وحدات الإدارة المؤسسية وطبقاتها المعتمدة نقلاً انتقائياً. بقيت الأرشيفات المرجعية دون تغيير.

## ما نُفذ

| المحور | صفحات الإدارة المنفذة أو المعتمدة | الربط الخلفي الفعلي |
| --- | --- | --- |
| A1 — الأمان والتدقيق | تسجيل الدخول، BFF، RBAC، النزاعات، سجل التدقيق | جلسة HttpOnly وCSRF، صلاحيات خادمية، سبب إلزامي وتدقيق لكل قرار مالي |
| A2 — دورة الطلبات والمالية | `/admin/orders`، `/admin/orders/[kind]/[id]`، `/admin/finance-suite` | بحث وفرز وتقسيم خادمي، timeline، إلغاء واسترداد وتعويض وإعادة إسناد وSLA وملاحظات داخلية، CSV خادمي، إيراد وعمولات وتسوية ودفعات وكشف مزود |
| A3 — التحليلات | `/admin/analytics-suite` | Funnels، cohorts، دوري مزودين، بحث، NPS، وتنبيهات z-score من تجميعات Mongo |
| A4 — CRM والخصوصية | `/admin/crm`، `/admin/segments`، `/admin/gdpr` | دليل مريض و360، شرائح compiled خادمياً، دورة تصدير/إخفاء GDPR حقيقية |
| A5 — المحتوى والنمو | `/admin/content-growth`، `/admin/home-curation`، `/admin/medicines-catalog` | مقالات ونشر/جدولة، كوبونات وقواعد استعمال، حملات Push وA/B، ترتيب الصفحة الرئيسية مدقق، والكتالوج الموجود مربوط بمساراته الحقيقية |
| A6 — تشغيل النظام | `/admin/system-ops` | أعماق BullMQ وإعادة محاولة، الترجمات، تحكم SEO، ورايات موحدة في مخزنين |
| A7 — مركز القيادة والتقارير | `/admin/command-center`، `/admin/scheduled-reports` | snapshot حي وSSE كل 15 ثانية، وجدولة/تشغيل/سجل تسليم التقارير من backend |

## إضافات خلفية مهمة

تمت إضافة أو إكمال عقد الملاحظات الداخلية للطلبات، وتصدير CSV خادمي لقائمة الطلبات، وفرز خادمي بالقيمة والتاريخ. كما أضيفت مسارات ترتيب الصفحة الرئيسية والرايات الموحدة، وربطت `feature_flags` بمسار `/api/v1/config` بحيث تصل حالة الراية ونسبة الإطلاق إلى تطبيقات العميل. تم كذلك توسيع محرك الحملات لدعم نسخة A/B وتوزيع ثابت حسب معرّف المستخدم، إضافة إلى دعم الشرائح المحفوظة في اختيار جمهور الحملة.

## التحقق المنفذ

| تحقق | النتيجة |
| --- | --- |
| تثبيت backend المقفل | `npm ci --ignore-scripts` نجح |
| بناء backend | `npm run build` نجح |
| تثبيت admin المقفل | `npm ci --ignore-scripts` نجح |
| بناء admin | `npm run build` نجح؛ تم توليد 59 مساراً |
| بوابة التكامل المؤسسية | **21/21** اختباراً ناجحاً، وتشمل A1–A7 والملاحظات والتصدير والحوكمة والرايات |
| فحص admin للبيانات الوهمية | لا تطابقات لـ `mock` أو `dummy` أو `fake` أو `fixture` أو placeholders خاصة بالبيانات |
| فحص تخزين المتصفح | لا استخدام لـ `localStorage` أو `sessionStorage` داخل `admin/src` |
| فحص الملفات المتتبعة | لا `node_modules` أو نواتج بناء متتبعة في Git |

> اختبار التقرير المجدول يثبت حساب payload حقيقي وتسجيل نتيجة التسليم. فشل التسليم في بيئة الاختبار متوقع لأن مزود البريد غير مهيأ، وقد سُجل كفشل تسليم بدلاً من اعتباره نجاحاً وهمياً.

## الالتزامات

| الالتزام | الوصف |
| --- | --- |
| `2d59ae17` | دورة الطلبات والمالية |
| `5756fb87` | التحليلات وCRM والشرائح وGDPR |
| `4277482a` | المحتوى والنمو وترتيب الصفحة الرئيسية وتحكم النظام |
| `6d12f06c` | مركز القيادة الحي والتقارير المجدولة |
| `217e3c7c` | اختبارات العقود الجديدة وربط الرايات بإعدادات العميل |

## متطلبات ما قبل النشر

يجب ضبط `ADMIN_BACKEND_URL` في مشروع لوحة الإدارة ليشير إلى backend المنشور، وضبط مفاتيح JWT وcookies الآمنة (`Secure` على HTTPS)، وتكوين مزود بريد فعلي للتقارير المجدولة. ينبغي أيضاً ضبط Redis وBullMQ وMongoDB وموفر Push في بيئة staging ثم تنفيذ اختبارات دخان للمصادقة وMoyasar وإرسال البريد والإشعارات. لا توجد بيانات أو أسرار إنتاجية في الفرع.

يحتاج مستهلكو `/api/v1/config` في تطبيقات العميل إلى احترام `feature_rollouts` إذا كان الإطلاق الجزئي بالنسبة المئوية مطلوباً في واجهة العميل نفسها؛ أما قيمة التفعيل الكلية فهي متاحة الآن في `features`.

## الخطوة المقترحة

ابدأ بنشر فرع التنفيذ إلى بيئة staging، ثم نفذ اختبار قبول يضم مديرين مختلفين لدفعة تتجاوز حد الاعتماد المزدوج، استرداداً جزئياً وكاملاً، تقريراً بريدياً، حملة A/B قصيرة، وتدفق GDPR. بعد ضبط المزوّدات الحية ونجاح الاختبار، يفتح طلب دمج من هذا الفرع إلى `main`.
