# ملف كونتكست مشروع نبض (Nabd) — دليل المبرمج الشامل

> آخر تحديث: 2026-08-04 — بعد EPIC 5 + الجولة الثانية (E5-R2: إغلاق ويب هوك fail-open، IDOR، بديل ريدس).
> هذا الملف يُسلَّم لأي مبرمج جديد ليفهم المشروع ويعمل عليه مباشرة دون تخمين.

---

## 1) ما هو المشروع؟

**نبض (Nabd)** منصة صحية سعودية شاملة (Super-App) تخدم المريض من الاستشارة حتى الدواء والتحاليل والأشعة والتمريض المنزلي والإسعاف، مع نظام ولاء وعائلة وتأمين.

المشروع = **4 تطبيقات + خادم**:

| التطبيق | التقنية | الوصف |
|---|---|---|
| `nabdah-backend` | NestJS 11 + MongoDB (Mongoose) + Redis + BullMQ | الـAPI الوحيد لكل التطبيقات |
| `nabd_plus` | Expo / React Native (expo-router) | تطبيق المريض (248 شاشة) |
| `NabdProvider` | Expo / React Native | تطبيق مقدم الخدمة (طبيب/صيدلية/مختبر/أشعة/تمريض/إسعاف — 42 شاشة) |
| `Napd-admin` | Next.js | لوحة تحكم الإدارة (35 صفحة) |

بالإضافة إلى خدمة FastAPI (`nabdah-fastapi`) للذكاء الاصطناعي/OCR تعمل بجانب الـbackend.

---

## 2) المستودع والبنية

- GitHub: `obaid08642-ops/new` — الفرع النشط: **`m7-quality`**.
- المستودع يتتبع **ملفات ZIP فقط** للتطبيقات الأربعة + هذا الملف (`PROJECT-CONTEXT.md`). مجلد `extracted/` مستثنى عبر `.gitignore`.
- عند العمل: فك الضغط محلياً → عدّل → أعد ضغط الـzip → commit.

```
nabdah-backend.zip      → src/modules (104 وحدات) + src/schemas (71 مخطط) + scripts
nabd_plus-patient.zip   → app/ (expo-router, 248 شاشة) + src/ (components, hooks, context, utils)
NabdProvider-provider.zip → src/screens (42) + src/api + src/context
Napd-admin-dashboard.zip  → Next.js pages/app (35 صفحة)
```

---

## 3) بيئة الإنتاج والنشر

- الخادم: OVH VPS — `57.131.133.208` (مستخدم `ubuntu`).
- كل الخدمات Docker Compose: ملف `/opt/nabdah/deploy/docker-compose.production.yml` مع `--env-file .env.production` (**إلزامي** — بدونه يفشل تعويض متغيرات Mongo ويحدث crash-loop).
- الحاويات: `nabdah-backend` (المنفذ الداخلي 8002 — لا يوجد port mapping للمضيف)، `nabdah-mongodb`، `nabdah-redis`، `nabdah-fastapi`، `nabdah-admin-web`، `nabdah-nginx`، `nabdah-livekit`، `nabdah-coturn`.
- النطاق: `https://api.nabd.plus/api/v1` خلف nginx (فيه rate-limit — الاستعلامات الخارجية الكثيرة ترجع 429؛ افحص من داخل الحاوية).
- قاعدة البيانات: MongoDB — قاعدة `nabd_nestjs`.

### خط أنابيب النشر (مُثبت ويعمل)

```bash
# 1) رفع المصدر
rsync -az --delete --exclude node_modules --exclude dist \
  -e "ssh -i <مفتاح النشر>" ./src-copy/ ubuntu@57.131.133.208:/home/ubuntu/backend-upload/

# 2) على الخادم: نسخ + بناء داخل حاوية node ثم بناء الصورة
sudo rsync -a --delete --exclude node_modules --exclude dist \
  /home/ubuntu/backend-upload/ /opt/nabdah/nabdah-backend/
sudo docker run --rm -v /opt/nabdah/nabdah-backend:/app -v npm-cache:/root/.npm \
  -w /app node:20-bookworm-slim sh -c "npm ci --legacy-peer-deps && npx nest build && npm prune --omit=dev"
cd /opt/nabdah/deploy
sudo mv /opt/nabdah/nabdah-backend/.dockerignore /opt/nabdah/nabdah-backend/.dockerignore.bak
sudo docker compose -f docker-compose.production.yml --env-file .env.production build backend
sudo mv /opt/nabdah/nabdah-backend/.dockerignore.bak /opt/nabdah/nabdah-backend/.dockerignore
sudo docker compose -f docker-compose.production.yml --env-file .env.production up -d --force-recreate backend
```

### محاذير مثبتة (تعلمناها بالطريقة الصعبة)

1. **`--env-file .env.production` إلزامي** في كل أوامر compose — لا يوجد ملف `.env`.
2. **توسيع المتغيرات داخل الحاويات**: `$MONGO_URL` داخل أمر `ssh "...docker exec..."` يتوسع في shell الخارجي (فارغ!). الحل: اكتب سكربت يقرأ `process.env`، انسخه `docker cp` إلى `/usr/src/app`، ثم `docker exec -w /usr/src/app nabdah-backend node script.js`.
3. **إعادة الإنشاء قد تُبقي اسم حاوية قديم مسبوقاً بالهاش** — أعد التسمية: `docker rename <hash>_nabdah-backend nabdah-backend`.
4. الفحص الحي يتم **من داخل الحاوية** (`wget http://127.0.0.1:8002/...`) وليس من المضيف.
5. مفتاح النشر محفوظ محلياً عند المالك — لا يُرفع للمستودع أبداً.

---

## 4) الـBackend — الخريطة الكاملة

### التقنية والبنية

- NestJS 11 + Mongoose (71 مخططاً في `src/schemas`) + Redis (rate-limit + OTP + queues) + BullMQ (push + notifications delivery).
- التوثيق: JWT (access قصير + refresh) — `src/common/auth.guard.ts` فيه `JwtAuthGuard` + `@Public()` + `@Roles()` + `@CurrentUser()`.
- الأحداث: `@nestjs/event-emitter` — العمود الفقري للتكامل بين الوحدات (الولاء، الإشعارات، التدقيق).
- الترجمة: `src/modules/i18n/i18n.service.ts` — المفاتيح inline `{ar,en,ur}` مع استيفاء `{param}`.

### خريطة الوحدات (104) حسب المجال

| المجال | الوحدات |
|---|---|
| النوايا/الحسابات | `auth`, `users`, `device-trust`, `security` (تدقيق+middleware) |
| المواعيد/الاستشارات | `care` (المواعيد+الحالات), `doctors`, `slot-locks`, `livekit`, `coturn`, `chat`, `socket`, `presence` |
| الصيدلية/الطلبات | `orders`, `pharmacy`, `pharmacy_ops`, `cart`, `medicines`, `prescriptions`, `ocr`, `returns` |
| التشخيص | `labs`, `radiology` (كلاهما state machine صارمة + تقارير PDF) |
| التمريض/الرعاية المنزلية | `home-care`, `home-care-compat`, `maternity`, `mental-health`, `nutrition` |
| الطوارئ/الإسعاف | `emergency` (+ `ambulance-fleet.controller` داخله) — محرك الإرسال الذكي S1 |
| المالية | `finance-engine` (قيود، maker-checker، استرداد، clawback), `payments` (Stripe/Tap/Moyasar), `moyasar`, `wallet`, `payouts`, `billing`, `webhooks` |
| التأمين | `insurance`, `insurance-engine` (NPHIES-ready، موافقات/رفض/copay) |
| الولاء/النمو | `loyalty`, `referral`, `ratings`, `community`, `family`, `articles` |
| الإشعارات | `notifications` (in-app + مستمعين), `push` (Expo/FCM/APNs عبر BullMQ), `sms`, `mail`, `admin-notification-center` (حملات+تذكيرات) |
| الذكاء الاصطناعي | `ai` (triage/OCR/تحليل تقارير/خطط تمارين — بوابة للـFastAPI) |
| الإدارة | `admin`, `admin-authority`, `admin-command-center`, `admin-governance`, `admin-web-core`, `approval-workflow`, `bans`, `feature-flags`, `config`, `business-rules` |
| مقدمو الخدمة | `provider`, `providers`, `provider-onboarding`, `provider-ops`, `provider-jobs`, `hospital`, `hospital-staff`, `facility-ops`, `drivers`, `recruitment` |
| البنية | `redis`, `storage`, `media`, `export`, `realtime`, `events`, `event-reliability`, `consistency`, `system-health`, `workflow-engine`, `booking-flow`, `booking-ops`, `unified-bookings`, `service-catalog`, `custom-services`, `home`, `search/seo*`, `timeline`, `tour`, `support`, `legal`, `legacy`, `compat`, `seed`, `medical-reports`, `medical-profile`, `health`, `patient-ux`, `api-security` (honeypots + blacklisting) |

### آلات الحالة (State Machines) — مصدر الحقيقة

| النطاق | الخريطة | المكان |
|---|---|---|
| طلبات الصيدلية | `ORDER_TRANSITIONS` | `src/common/enums.ts` — مفروضة في `orders.service` |
| الطوارئ | `EMERGENCY_TRANSITIONS` | `enums.ts` — مفروضة في `emergency.service.transition` |
| الوصفات | `PRESCRIPTION_TRANSITIONS` | `enums.ts` |
| المواعيد | `APPT_STATES` + خريطة | `care/appointments.service.ts` |
| المختبر | `LAB_BOOKING_TRANSITIONS` | `schemas/lab.schema.ts` — مفروضة في `labs.service.transition` + حراس على uploadReport/updateSampleStage/declareEmergency (EPIC5) |
| الأشعة | `RADIOLOGY_BOOKING_TRANSITIONS` | `schemas/radiology.schema.ts` — مفروضة في `transition` + حراس على abortScan/processInsuranceApproval (EPIC5) |
| التمريض | عبر `workflow-engine` | `home-care.service.transition` |
| دورة موحدة | `UNIFIED_TRANSITIONS` (7 حالات) | `workflow-engine` يعيّن المفردات الداخلية إليها |

**القاعدة**: أي تغيير حالة جديد يجب أن يمر عبر `transition()` أو يتحقق من الخريطة — ممنوع الكتابة المباشرة على `state` دون حارس (EPIC5 أغلق 5 مسارات كانت تفعل ذلك).

### الأحداث المهمة (من يُطلق ومن يستمع)

| الحدث | المُطلق | المستمعون |
|---|---|---|
| `health.vitals_logged` | `health.service.addVital` (EPIC5 — كان ميتاً) | `loyalty` (+10 نقاط، سقف 5/يوم) |
| `loyalty.points_awarded` | `loyalty.awardPoints` | `notifications` (ترقية مستوى/إكمال تحدٍ فقط) |
| `insurance.decided` | `insurance-engine`, `provider.controllers` | `notifications` (EPIC5) + `security` (تدقيق EPIC5) |
| `finance.operation.executed/rejected` | `finance-engine` maker-checker | `notifications` (payout/استرداد EPIC5) + `security` |
| `payment.completed` | `payments` | `push` + `notifications` (EPIC5) + `security` + workflow |
| `payment.refund` | `finance-engine` | `security` + إشعار مباشر للمريض |
| `medical_report.created` | labs/radiology/reports | `notifications` + `security` (EPIC5) |
| `emergency.*` | `emergency.service` | notifications/push/security |
| `admin.user_updated/provider_approved/provider_rejected/user_deleted` | admin controllers (EPIC5 أضافها للمسارات القديمة) | `security` (تدقيق) |
| `community.comment_added`, `family.*`, `referral.*`, `ai.triage_completed` | وحداتها | `notifications` (EPIC4) |

### نظام الولاء

- المستويات: bronze/silver/gold/platinum (`calculateTier` من lifetime_points).
- جدول النقاط `POINTS_TABLE`: booking_completed=50, order_delivered=30, review_submitted=20, referral_converted=100, referral_welcome=50, vitals_logged=10.
- التحديات: `target_action` يجب أن يساوي أحد أسباب الجدول. الانضمام محفوظ (`POST /loyalty/challenges/:id/join`) و`joined` يظهر في الجلب.
- مكافحة الاستغلال: idempotency (reason+ref)، سقف vitals 5/يوم، TTL للنقاط 365 يوماً.
- البذر: `scripts/seed-loyalty.ts` (5 تحديات + 5 مكافآت — منفذ في الإنتاج).

### محرك الإرسال الذكي للإسعاف (EPIC5/S1)

- `EmergencyService.autoDispatch(id)` يعمل تلقائياً بعد كل SOS وأيضاً يدوياً `POST /emergency/:id/auto-dispatch` (أدمن).
- التقييم: القرب/ETA (last_location) + توفر الوحدة + نوعها (BLS/ALS/ICU مقابل severity) + تقييم المقدم (provider_profiles.rating_avg) + عبء العمل الحالي + أولوية المستشفى (env `AMBULANCE_HOSPITAL_PRIORITY_BONUS`).
- **ملكية المقدم داخلية فقط**: المريض يرى `assigned`, `unit_label` (اللوحة), `paramedic_name`, ETA, التتبع — ولا يرى `assigned_hospital_id`/`assigned_provider_id`/`assigned_ambulance_id` أبداً (`patientView()`).

---

## 5) تطبيق المريض (`nabd_plus`)

- expo-router — كل شاشة ملف تحت `app/`. المكونات المشتركة في `src/components` (Icon ثابت مع fallback لـMaterialCommunityIcons).
- `src/utils/api.ts` — `apiFetch`: توكن من SecureStore، مهلة 20 ثانية، خطأ `OFFLINE_ERROR`/`TIMEOUT_ERROR`، **إعادة محاولة واحدة تلقائية لـGET فقط** (ممنوع إعادة mutations حمايةً من التكرار).
- الإشعارات: `src/hooks/usePushNotifications.ts` — `routeFromNotificationData` + `translateBackendRoute` (مفردات الباكند `/tracking/:kind/:id` → مسارات التطبيق + قائمة `VERBATIM_ROUTES`). شاشة الإشعارات نفسها تستخدم نفس الترجمة (EPIC5 — كانت تدفع المسار الخام وتكسر).
- حالات الشاشات: `src/components/ScreenStates.tsx` (Loading/Empty/Error/Retry) — كل شاشة API يجب أن تعرض الأربع حالات، بلا شاشة بيضاء.
- تصميم صادق: لا بيانات مفبركة — أي قائمة فارغة = empty state صادق (EPIC4 أزال 7 شاشات/تدفقات مفبركة).

### تدفقات حرجة

- الدفع: `payments/intent/:type/:id` (idempotent — يرجع الموجود، ويرفض `booking_already_paid`) → verify → webhook.
- الحجز: مواعيد عبر `slot-locks`؛ مختبر/أشعة/تمريض فيهم حارس تكرار (نفس المريض+الخدمة خلال 3 دقائق = يرجع الحجز الأصلي).
- الطوارئ: `POST /emergency/trigger` → شاشتا `sos-active` + `tracking` (polling 10 ثوانٍ، يرى وحدة/ETA/خطوات فقط).

---

## 6) تطبيق مقدم الخدمة (`NabdProvider`)

- Expo منفصل — `src/api/provider.ts` + `src/context` (توثيق مستقل عن تطبيق المريض).
- 42 شاشة، كل الشاشات التي تستدعي API فيها loading/error (مفحوص في EPIC5/S9).
- مزدوج النموذج للإسعاف: مقدم مستقل أو مستشفى بأسطول (`provider/ambulance/fleet` CRUD + مراجعة أدمن قبل الظهور).
- التسجيل عبر `provider-onboarding` — ليس عبر `/auth/register` العام.

---

## 7) لوحة الإدارة (`Napd-admin`)

- Next.js — تتصل بنفس الـAPI عبر `/admin/*` + `/admin-authority` + `/admin-web-core`.
- مراجعة أسطول الإسعاف، الوساطة، الحملات، maker-checker المالية، سجلات التدقيق.

---

## 8) قاعدة البيانات — المجموعات الأساسية

`users` (password_hash bcrypt $2a$12$, role, active, suspended, verified) • `patient_profiles` • `provider_profiles` (rating_avg, type) • `appointments` (slot_start/slot_end، حالات UPPERCASE) • `orders` • `labbookings` / `radiologybookings` / `homecarebookings` (state_history[]) • `emergency_requests` • `ambulance_vehicles` (vehicle_type BLS/ALS/ICU، last_location، status يُراجعه الأدمن) • `insurance_companies` (code, name_ar, name_en, is_active — هُوجرت في EPIC4) • `loyalty_accounts/transactions/challenges/rewards` • `medicalreports` • `articles` • `community_posts` • `family_groups` • `wallet_wallets/transactions` • `moyasar_payments` • `finance_operations` (pending_approval → executed/rejected) • `audit_logs` • `notifications` (title_key/body_key + action:{route,payload}).

---

## 9) الاختبار والتشغيل محلياً

```bash
# backend
npm ci --legacy-peer-deps
npx tsc --noEmit
npx jest                 # 24 جناحاً / 198 اختباراً — يجب أن يبقى أخضر
npx nest build

# patient / provider
npm ci --legacy-peer-deps
npx tsc --noEmit
npx expo export --platform android   # التصدير للويب يفشل بسبب react-native-maps — استخدم android

# ملاحظة بيئة العمل الرملية للوكيل: /tmp يُمسح بين الجلسات — عدّل دائماً في /mnt ثم rsync + npm ci
```

**بوابة الإرسال الإلزامية**: backend tsc + jest أخضر، tsc للتطبيقين، expo export للمريض، نشر + فحص حي قبل أي "تم".

---

## 10) القواعد الذهبية (غير قابلة للتفاوض)

1. **لا بيانات وهمية في مسارات الإنتاج** — بيانات حقيقية أو حالة فارغة/خطأ صادقة مع إعادة محاولة.
2. **لا أسرار في الكود أو git** — المفاتيح في env فقط؛ أخفِ أي توكن في المخرجات.
3. **Mongo**: لا تخلط مفاتيح عارية مع مُعامِلات `$` في نفس التحديث.
4. **الملكية أولاً**: أي endpoint يمس بيانات مستخدم يتحقق من الملكية/الدور (IDOR ممنوع).
5. **الحالات عبر transition()** فقط، والتاريخ في `state_history`.
6. **أي إجراء مالي/طبي حساس = سجل تدقيق** (`security` module) + إشعار بعمق ربط صحيح.
7. **أي حقل من العميل = whitelist** (الـmass-assignment أُغلق في `/users/me/profile` — طبّق نفس النمط في أي PATCH جديد).
8. **التسجيل العام**: القائمة البيضاء `SELF_REGISTERABLE` في `auth.service.register` — ممنوع إضافة أدوار موظفين/إدارة إليها أبداً.

---

## 11) ما أُنجز (EPIC 1→5)

| EPIC | الملخص | Commit |
|---|---|---|
| 1 | محرك المالية والسوق (محفظة/استرداد/عمولات/maker-checker) — 28/28 فحص حي | R16/… |
| 2 | تدقيق تطبيق المريض (مركز الطلبات، تقييمات، إزالة فبركة، أداء) | `d00f747` |
| 3 | تطبيقات المقدمين (ربط صادق، وساطة أدمن، إسعاف تسجيل) | `adeebed` |
| + | النموذج المزدوج للإسعاف + تتبع حقيقي | `f81a351` |
| 4 | المنظومة الصحية/العائلة/الولاء/التفاعل (تحديات محفوظة، بحث 9 نطاقات، 10 سيناريوهات إشعار، إصلاح كرون تذكيرات ميت، إزالة 7 فبركات، 18 شركة تأمين مُهاجرة، حلقة vitals→ولاء كانت ميتة) | `49dfb34` |
| 5R2 | الجولة الثانية (E5-F1→F6): ويب هوك fail-closed (moyasar/paytabs/sms) + timing-safe + منع replay (Redis SETNX 24h)، إغلاق IDOR في `/payments/booking/:type/:id` + الاسترداد أدمن فقط + ملكية verifyPayment، graceful shutdown، Swagger مغلق بالإنتاج (SWAGGER_ENABLED)، CORS الافتراضي nabd.plus، خنق المصادقة (login 10/min، OTP 5/min) + نقاط AI المدفوعة، سقف رفع AI 10MB + فلاتر، **إصلاح بديل ريدس** (انقطاع ريدس كان = انقطاع API كامل — كُشف حياً)، dedup لتسليم الإشعارات — 198/198 jest (24 جناحاً) | `eaa1e76` |
| 5 | تصليحات الإنتاجية: إرسال إسعاف ذكي + إخفاء الملكية، 5 انتقالات حالة مكشوفة، 5 مستمعي إشعارات + إصلاح عمق الربط داخل التطبيق، timeout/retry للشبكة، منع الدفع المزدوج والحجز المكرر، سجلات تدقيق ناقصة، **إغلاق تصعيد صلاحيات P0** (التسجيل العام كان يقبل role=admin)، تحويل محفظة ذري، إغلاق mass-assignment، حد قوائم الأطباء | `eaabe99` |

---

## 12) عناصر مفتوحة على المالك (لا حل برمجياً لها)

1. **Moyasar**: الحساب الحي غير مفعّل («Entity not activated») — مدفوعات البطاقات الحية متوقفة حتى تفعيل الكيان من لوحة Moyasar.
2. حسابا اختبار في الإنتاج أنشأتهما للفحص الحي: `epic4.tester@nabd.plus` و`probe.patient@nabd.plus` — احذفهما عند الانتهاء أو اطلب حذفهما.
3. بيانات مرجعية تحتاج إثراءً تدريجياً من الإدارة: أسطول الإسعاف (اعتماد المركبات + مواقعها الحية ليعمل الإرسال الذكي بكامل دقته)، صيدليات/أطباء حقيقيون.

---

## 13) أين تجد ماذا بسرعة

| تريد | المكان |
|---|---|
| إضافة إشعار جديد | مستمع `@OnEvent` في `notifications.service.ts` + مفاتيح i18n + `action.route` + تأكد أن `translateBackendRoute` يعرفه |
| إضافة حالة لخدمة | خريطة الانتقالات في `enums.ts`/مخطط الخدمة + فرضها في `transition()` |
| إضافة حدث مالي | عبر `finance-engine` operations (maker-checker) — ليس مباشرة |
| سجلات التدقيق | مجموعة `audit_logs` عبر `security` module |
| فحص حي | سكربت داخل الحاوية (انظر §3 محاذير) |
| بذر بيانات | `scripts/seed-*.ts` — التجريبي محجوب خلف `SEED_DEMO_DATA=true` (غير مفعّل في الإنتاج) |
