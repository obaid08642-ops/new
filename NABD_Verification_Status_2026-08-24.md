# حالة التحقق الفني — 24 أغسطس 2026

## النتيجة التنفيذية

نجحت اختبارات المصدر والبناء لهذه الدفعة، لكن **لا يوجد اعتماد للدمج أو النشر**. المسارات التي عولجت الآن أفضل تقييدًا ووضوحًا من السابق، إلا أن التحقق المتعدد الأدوار على بيئة Mongo وRedis مؤقتة لم يكتمل بسبب تنزيل MongoDB الثنائي المطلوب لـMongoMemoryServer بسرعة غير عملية داخل بيئة الاختبار. أوقفت المحاولة ونظفت Redis المحلي المؤقت؛ لم تُستخدم أي بيانات أو أسرار إنتاجية.

| بوابة التحقق | النتيجة | الدليل |
|---|---:|---|
| بناء NestJS | PASS | `88_backend_build_after_sentry_remediation.log` |
| اختبارات الباكند مقسمة منخفضة الذاكرة | PASS | 97 suites و505 tests عبر السجلات `100` إلى `104` |
| اختبارات إصلاحات Sentry المستهدفة | PASS | 10 suites و50 tests في `92_backend_sentry_remediation_targeted_suite.log` |
| TypeScript للتطبيق المحمول | PASS | سجلات `84`, `89` إلى `91`, و`94` إلى `97` |
| اختبارات التطبيق المحمول الكاملة | PASS | 33 suites و73 tests في `98_mobile_full_regression_after_insurance_emergency.log`، بلا `forceExit` |
| readiness إنتاجي قراءة فقط | PASS لحظيًا | `GET /api/v1/health/readiness` أعاد MongoDB وRedis `up` في 12:41 UTC |
| أحداث Sentry production في آخر 24 ساعة | لا نتائج | الاستعلام المقيد بـ`environment:production level:error` لم يرجع أحداثًا وقت الفحص |
| E2E Mongo+Redis+HTTP متعدد الأدوار | UNVERIFIED | boot احتاج تنزيل MongoDB 7.0.24 (81.8 MB) وتوقف عند تقدم بطيء؛ لم تُشغّل المصفوفة |
| PSP sandbox/live | BLOCKED | Sentry سجّل أن حساب Moyasar الحي غير مفعّل؛ الكود الآن fail-closed |

## أثر التحقق على القرارات

لا تعني جاهزية health أو خلو Sentry في نافذة قصيرة أن بنية Mongo أو Redis صارت موثقة أو أن الإصدار الحي يساوي فرع الإصلاح. كما لا تثبت الاختبارات الوحداتية أن مريضًا وصيدلية ومندوبًا، أو مريضًا وطبيبًا، أو مريضًا ومركز تشخيصات، أكملوا الرحلة نفسها ضد قاعدة مؤقتة وحسابات مصرح لها.

لذلك يظل القرار الصحيح هو **عدم دمج الفرع في `main`، وعدم النشر** إلى أن تكتمل مصفوفة E2E في بيئة sandbox منفصلة، وتتأكد خطة النسخ الاحتياطي والاسترجاع، ويُعتمد PSP sandbox ثم live، ويعطي المستخدم موافقة صريحة منفصلة على خطة نشر محددة.

## الالتزام المرجعي

تم حفظ هذه الدفعة المصدرية في فرع المراجعة فقط:

> `80420c6 fix: harden production workflows and payment capabilities`

لا يتضمن هذا الالتزام أي نشر أو merge إلى `main`.
