# حالة التحقق التشغيلي — 24 أغسطس 2026

## النتيجة التنفيذية

> **حالة فرع المراجعة: تحقق sandbox ناجح، لكن الدمج إلى `main` والنشر إلى الإنتاج ما زالا محظورين.**

اكتملت الآن مصفوفة E2E متعددة الأدوار على باكند مبني محليًا مع MongoDB وRedis مؤقتين فقط. شملت المصفوفة المصادقة و2FA، الاستشارات، دورة حالة الموعد، التأمين، الصيدلية، المختبر، الأشعة، الرعاية المنزلية، الإدارة، الإشعارات، Socket.IO، LiveKit، SEO، والاستردادات. النتيجة النهائية هي **67 ناجحًا، 0 فاشل، 0 متخطى** في السجل `137_e2e_matrix_rerunnable_unique_lab_provider.log`.

هذه نتيجة قوية لعقود الباكند والرحلات التي غطتها المصفوفة، وليست دليلًا على جاهزية PSP الحي أو تكافؤ بنية الإنتاج أو جاهزية نشر. لا تحتوي بيئة الاختبار على بيانات أو أسرار إنتاجية؛ يستخدم harness حسابات اختبار وMongoMemory وRedis محليين فقط.

| بوابة التحقق | الحالة | الدليل |
|---|---:|---|
| بناء NestJS بعد الإصلاحات الأخيرة | **PASS** | `131_backend_build_after_active_radiology_route_fix.log` |
| اختبارات انحدار الباكند الكاملة، خمس دفعات تسلسلية | **PASS** | 98 suite و509 test: السجلات `140`–`144` |
| اختبارات الإصلاحات المستهدفة | **PASS** | 5 suites و25 test في `138_targeted_regressions_after_service_hardening.log` |
| E2E متعدد الأدوار عبر Mongo+Redis+HTTP | **PASS** | 67 passed / 0 failed / 0 skipped في `137_e2e_matrix_rerunnable_unique_lab_provider.log` |
| قابلية إعادة E2E | **PASS** | حسابات مرضى/مدير، معاملة copay، نافذة استشارات، ومزوّد مختبر فريدة لكل تشغيل؛ لا تنظيف يدوي مطلوب |
| TypeScript للتطبيق المحمول | **PASS سابقًا** | سجلات `84`, `89`–`91`, و`94`–`97`؛ لم يتغير مصدر المحمول في هذه الدفعة |
| اختبارات التطبيق المحمول | **PASS سابقًا** | 33 suites و73 test في `98_mobile_full_regression_after_insurance_emergency.log` |
| اختبارات Patient Web وبناؤه | **PASS سابقًا** | 140 ملفًا ناجحًا / 274 اختبارًا ناجحًا، ثم build ناجح في السجلين `109` و`110` |
| readiness إنتاجي قراءة فقط | **PASS لحظيًا** | `GET /api/v1/health/readiness` أعاد MongoDB وRedis `up` عند 12:41 UTC؛ ليس دليل نشر |
| Sentry production، نافذة 24 ساعة وقت الفحص | **لا أحداث** | استعلام `environment:production level:error` لم يرجع أحداثًا وقت الفحص؛ لا يعني حل الجرد التاريخي |
| PSP sandbox/live وwebhook موقّع | **BLOCKED** | حساب Moyasar الحي غير مفعل وفق Sentry؛ الكود fail-closed ولا توجد معاملة PSP sandbox/حي موثقة |

## ما الذي أثبته E2E فعلًا

| المجال | ما تم التحقق منه | الحالة الدقيقة |
|---|---|---|
| الدخول والأمان | تسجيل، دخول، رفض NoSQL injection، كلمة مرور ضعيفة، 2FA مدير دون استخراج OTP من Redis أو السجلات | **PASS في sandbox** |
| الاستشارات | clinic/video/home مع cash/card/insurance وفق القيود، انتقالات confirm/check-in/start/complete، إلغاء، منع الغريب، إعادة الجدولة | **PASS في sandbox؛ PARTIAL للإنتاج** لأن card يثبت حالة pending فقط ولا يثبت PSP أو webhook حي |
| التأمين | إنشاء من حجز مملوك، موافقة كاملة وجزئية، copay بمعاملة متحقق منها، رفض مع سبب، منع قرار المريض، queue الإدارة | **PASS في sandbox؛ PARTIAL للإنتاج** |
| الصيدلية | إنشاء طلب، قائمة طلباتي، بحث دواء منشور وفق بوابات الحوكمة | **PARTIAL**؛ لا يثبت fulfillment صيدلية أو وصفة/Rx أو دفع PSP أو توصيل فعلي |
| المختبر | خدمة منشورة، حجز منشأة نقدي، قائمة حجوزات المريض المملوكة | **PARTIAL**؛ لم تثبت رحلة مزوّد المختبر، جمع العينة، QC أو التقرير النهائي في هذه المصفوفة |
| الأشعة | خدمة منشورة، حجز مركز، قراءة حجز المريض، فرض الخدمة/السعر/الاسم من الكتالوج بدل قيم العميل | **PARTIAL**؛ لم تثبت قبول المركز أو الفحص أو تقرير طبي نهائي |
| الرعاية المنزلية | خدمة منشورة، حجز مريض نقدي، قراءة مملوكة وإلغاء | **PARTIAL**؛ لم تثبت قبول ممرض أو التتبع أو بدء/إنهاء الزيارة في هذه المصفوفة |
| الإدارة والإشعارات | قوائم الإدارة والصلاحيات، رفض الطبيب/المريض، إشعار مجدول، قائمة إشعارات | **PASS في sandbox** |
| الاتصال الفوري | Socket يرفض JWT الفارغ ويقبل الصحيح؛ اتصال LiveKit يربط المريض بالطبيب عبر `doctor_user_id` | **PASS في sandbox؛ UNVERIFIED لخدمة LiveKit الخارجية** |
| SEO | sitemap لطبيب منشور، meta fallback، `llms.txt` | **PASS في sandbox** |
| الاسترداد | نافذتا 50% و100% ومنع طلب بلا سبب | **PASS في sandbox** |

## إصلاحات هذه الدفعة بعد نتيجة E2E الأولية

أصبح رمز 2FA قابلاً للاختبار فقط عند اجتماع `E2E_MODE=true` و`E2E_OTP_CODE` صالح؛ يبقى رمز الإنتاج عشوائيًا ومخزنًا كـbcrypt hash فقط ولا يطبع في السجلات. كما قُيِّد bypass rate-limit داخل API security بعلامتي E2E الصريحتين واختبر انحداريًا.

صُحح تفويض LiveKit ليستعمل `doctor_user_id` الخاص بحجز الاستشارة بدل `doctor_id` الخاص بملف المزوّد، لأن الأخير ليس هوية حساب مصادق عليها. كذلك أضيف عقد حجز مريض محدود للرعاية المنزلية تحت `/nursing/bookings` مع القراءة والإلغاء عبر طبقة الخدمة المقيّدة بالملكية.

أظهر E2E تعارض مساري أشعة؛ المسار الفعلي هو controller حجز مركز الأشعة. تم إصلاحه ليقبل خدمة منشورة ومراجعة طبيًا فقط، وموعدًا مستقبليًا، ويطبق قيد التوصيل/الدفع/الإحالة، ويأخذ السعر واسم الفحص ونوعه من كتالوج الخادم. يؤكد E2E أن payload عميل بقيمة `total=1` واسم/نوع مزيفين ينتهي بحجز مملوك بقيمة الكتالوج الصحيحة `150` واسم `Chest X-Ray`.

## ما لا يزال يمنع الدمج أو النشر

لا يجوز استنتاج أن PSP جاهز من أن حجز البطاقة يصل إلى `PENDING`. يلزم تشغيل Moyasar sandbox بتفويض صالح، وتنفيذ checkout حقيقي، وإثبات webhook موقّع ومدفوع idempotently، ثم استكمال تفعيل live بشكل منفصل. كما يجب قبل الإنتاج تأكيد تخطيط المصدر الذي تبني منه بيئة النشر، مراجعة متغيرات Mongo/Redis وDNS على الخادم، اختبار migration جاف، إثبات نسخة احتياطية واسترجاع، كتابة rollback، ثم الحصول على **موافقة المستخدم الصريحة والمنفصلة فورًا قبل النشر**.

يبقى جرد Sentry التاريخي مرجعًا مفتوحًا ولا يجوز حل أو تجاهل issues منه لمجرد أن نافذة 24 ساعة لم ترجع أحداثًا. يلزم تحقق عدم تكرار مرتبط بإصدار قابل للتعريف بعد نشر مضبوط.

## قرار الفرع

الالتزام السابق لفرع المراجعة هو `8ffbd0f`. لم يُدمج أي شيء إلى `main` ولم يُنشر أي شيء. ستضاف الإصلاحات ونتائج التحقق الحالية إلى **فرع المراجعة نفسه فقط** بعد فحص الأسرار والفروق، ولا تغير هذه الوثيقة قرار حظر النشر.

## مراجع الأدلة المحلية

| المرجع | الغرض |
|---|---|
| `137_e2e_matrix_rerunnable_unique_lab_provider.log` | النتيجة النهائية للمصفوفة: 67/0/0 |
| `138_targeted_regressions_after_service_hardening.log` | الانحدار المستهدف: 5 suites / 25 test |
| `140_backend_regression_batch_1.log` إلى `144_backend_regression_batch_5.log` | الانحدار الكامل المتسلسل: 98 suite / 509 test |
| `131_backend_build_after_active_radiology_route_fix.log` | آخر بناء NestJS ناجح |
| `NABD_Service_Journey_Audit_2026-08-24.md` | حدود اكتمال كل رحلة ومتطلبات الإنتاج |
| `NABD_Sentry_Production_Inventory_2026-08-24.md` | جرد أخطاء Sentry التاريخية وحواجزها |
