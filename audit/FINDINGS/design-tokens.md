# P6-D (1): استخراج التوكنز وتوحيد المصدر + Benchmark (إضافة 23)
> المصدر الوحيد: `packages/design-tokens/tokens.json` v1.0.0. المرايا يجب أن تطابقه.

## الجرد (2026-09-12)
| المنصة | النظام السابق | النضج |
|---|---|---|
| patient-app | `src/theme/{brand,colors,ThemeEngine}.ts` — lime/mint/coral/navy + light/dark | ✅ الأصل (استُخرجت منه القيم) |
| patient-web | `app/globals.css` — 66 متغيراً (brand-*/color-*/radius/shadow/motion/space) + dark | ✅ يعكس البراند (يُطابق tokens.json) |
| provider-app | ألوان Material مبعثرة (`#2196F3/#FF9800/#F44336/#9C27B0/#4CAF50`) بلا ثيم | ❌ دين — أُنشئ `src/theme/tokens.ts` (مرآة) |

## تباينات يجب حسمها (قرارات مالك)
1. **الداكن في الويب يستخدم cyan `#22c0d4` كأساسي** (`globals.css:290`) بينما الموبايل lime `#B8E030` — التوحيد المقترح: lime في كل مكان (tokens.json). يحتاج اعتماداً (يغير شكل الوضع الداكن ويب).
2. مفاتيح الموبايل المختصرة (`p/pd/bd/t2...`) مقابل أسماء الويب الوصفية — التوحيد المقترح: الأسماء الوصفية عند أي لمس مستقبلي (لا إعادة تسمية جماعية الآن — خطر كسر).

## Benchmark (المرجعيات العالمية — إضافة 23)
| المرجع | ما نأخذه |
|---|---|
| Apple Health | تباين هادئ + بطاقات تنفس + Dynamic Type (خطوط ديناميكية — إضافة 22) |
| One Medical | تسلسل حجز قصير + لغة مطمئنة (يدعم J1–J3) |
| Calm | حركة بطيئة مقصودة (motion.slow=400) + مساحات |
| Ada | فرز أعراض بحالات فارغة ممتازة (تنطبق على health) |
| Uber | تتبع حي + حالات طلب دقيقة (تنطبق على order-tracking) |

## موجات الترقية (التالي بعد الاعتماد)
- **Wave 2**: تحويل شاشات المزود للاستيراد من `theme/tokens.ts` (إزالة Material hex) — دفعات حسب الدور.
- **Wave 3**: قرار cyan-vs-lime الداكن + تطبيقه.
- **Wave 4**: ترقية مكون-بمكون (skeletons/60fps/haptics/dark) + visual regression.
- مؤجل صريح: توحيد Expo v54→v57 (وظيفي خطر — رزمة منفصلة مع regression كامل).
