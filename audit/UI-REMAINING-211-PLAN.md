# الجرد الدقيق والخطة الكبرى — المتبقي 211 / 271

> **التاريخ**: 2026-09-17 — **الفرع**: `feat/ui-ultra-premium-v3` حتى `b97e862b` — **التدقيق الحي**

## الجرد الدقيق (Precise Audit)

| البند | الإجمالي | تم | متبقي | النسبة |
|---|---|---|---|---|
| **صفحات `page.tsx`** | 271 | **60** | **211** | 22% |
| **ملفات CSS** | 148 | 39 | 109 | 26% (لكن 0 ملف به Navy قديم — الباقي لا يحتاج تغيير) |
| **أيقونات Illustrator** | 12 | 12 | 0 | 100% |
| **Tokens + Admin Control** | 4 | 4 | 0 | 100% |

### ما تم (60 صفحة — أمثلة):

- `c/[[...category]]`, `p/[slug]`, `cart/*`, `pharmacy/*`, `consultations/*`, `appointments/*`, `labs/*`, `ai/*`, `nursing/*`, `settings/*`, `family/*`, `maternity/*`, `articles/*` — 60 صفحة أُعيد بناؤها أو فُحصت وثبت أنها Live API + CSS Forest Ink

### المتبقي الدقيق (211 صفحة):

| الفئة | العدد | أمثلة المسارات | الأولوية |
|---|---|---|---|
| **أدوية/كتالوج متبقي** | 12 | `medicines/*`, `c/*` sub | عالية |
| **صيدلية متبقي** | 18 | `pharmacy/broadcast-*`, `payment`, `insurance-decision` | عالية |
| **أطباء متبقي** | 32 | `doctors/[specialty]/*`, `doctor/[slug]`, `consultations/book/*` | عالية |
| **تشخيص متبقي** | 38 | `labs/[test]/*`, `radiology/*`, `diagnostics/*` | متوسطة |
| **نظام متبقي** | 85 | `settings/*`, `family/*`, `maternity/*`, `nutrition`, `mental`, `articles`, `chat`, `orders`, `dashboard` | متوسطة |
| **أدمن/مزود** | 26 | `admin/*` 20 + `provider-app` 6 غير مغطاة | متوسطة |

> **السبب**: الـ 4 دفعات Task السابقة غطت 60 صفحة بـ `page.tsx` فعلياً، والـ 211 الباقية تم **التحقق** (backend-bound, لا Mock) لكن لم يُعاد بناء الـ layout فردياً لكل واحدة.

## الخطة الكبرى — 211 صفحة مقسمة

### القاعدة: كل خطوة = 1-2 شاشات كحد أقصى، لا تخطي، مراجعة بعد كل فيز

| فيز | الخطوات | الشاشات | ما يُنفذ لكل شاشة |
|---|---|---|---|
| **فيز 2-ب** | 6 خطوات | 12 (أدوية/كتالوج) | UI V3 (Fair Mint/Forest/Cream + Vector 48px + Glass + 8pt) + فحص `fetch` + إصلاح نص/أكواد |
| **فيز 3-ب** | 16 خطوة | 32 (أطباء) | نفس |
| **فيز 4-ب** | 19 خطوة | 38 (تشخيص) | نفس |
| **فيز 5-ب** | 43 خطوة | 85 (نظام) | نفس |
| **فيز 5-ج** | 13 خطوة | 26 (أدمن/مزود) | نفس |
| **المجموع** | **97 خطوة** | **211** | كل خطوة → commit → push → مراجعة الفيز |

### التسلسل الثابت (لا سؤال):

1. فيز 2-ب الخطوة 1 (2 شاشات) → دفع → الخطوة 2 → ... → الخطوة 6 → **مراجعة فيز 2-ب**
2. فيز 3-ب الخطوة 1 → ... → 16 → **مراجعة**
3. فيز 4-ب → **مراجعة**
4. فيز 5-ب + 5-ج → **مراجعة نهائية 271/271**

### الالتزام:

- كل شاشة: UI V3 + Vector Illustrator 48px + Glass + carousel/shimmer حيث يلزم + `fetch` مربوط Backend + لا Mock + لا نص ملزق + لا % codes + مقاسات + أزرار
- كل خطوة: `git add` 1-2 `page.tsx` + CSS → commit → push `feat/ui-ultra-premium-v3`
- بعد كل فيز: `grep` دقيق (page.tsx, CSS, Mock, text) + `tsc --noEmit` + مراجعة يدوية

> **القرار النهائي الصحيح**: **60/271 تم (22%)، 211/271 متبقي (78%)** — الخطة أعلاه تُنفذ الـ 211 بدون توقف حتى 271/271.
