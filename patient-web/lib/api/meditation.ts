export type MeditationSessionSummary={id:string;type?:string;durationMinutes?:number;completed?:boolean;loggedAt?:string};
export function parseMeditationHistory(payload:unknown):MeditationSessionSummary[]{const rows:unknown[]=Array.isArray(payload)?payload:payload&&typeof payload==="object"&&Array.isArray((payload as Record<string,unknown>).data)?(payload as Record<string,unknown>).data as unknown[]:[];return rows.flatMap((value,index)=>{if(!value||typeof value!=="object"||Array.isArray(value))return[];const row=value as Record<string,unknown>;return [{id:typeof row.id==="string"&&row.id.length<=160?row.id:`meditation-${index}`,type:typeof row.type==="string"?row.type:undefined,durationMinutes:typeof row.duration_minutes==="number"?row.duration_minutes:undefined,completed:typeof row.completed==="boolean"?row.completed:undefined,loggedAt:typeof row.logged_at==="string"?row.logged_at:undefined}];});}
