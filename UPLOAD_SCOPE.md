# Isolated workstation source upload

> Source revision: `51a84c76a690f30baac8b4bb3df6ab575aad4520`. This is an **isolation branch**, not a merge candidate and not evidence of runtime readiness.

The snapshot was built from tracked `HEAD` only. It excludes dependencies, build artifacts, all uncommitted changes, the absent Provider symlink target, and the two mobile service-configuration files pending credential review. See `WORKSTATION_UPLOAD_MANIFEST.json` for hashes and the exact file list.
