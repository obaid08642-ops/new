# تقرير Recovery & Failure — نبض
**التاريخ:** 2026-07-31 · سلوك النظام تحت الفشل — مُوثَّق من تحققات حية

## ١) انقطاع الإنترنت

| أثناء | سلوك النظام | فقد بيانات؟ | استكمال؟ |
|---|---|---|---|
| **الدفع** | الطلب يُنشأ PENDING_PAYMENT — يُستكمل لاحقاً أو retarget reminder (cron 6h) | لا | نعم |
| **المكالمة** | Socket reconnect تلقائي + ICE renegotiation (LiveKit) + CallKit باقٍ | لا | نعم (reconnect مُتحقق) |
| **رفع الروشتة** | الرفع محلي state — يُعاد عند عودة النت (offline banner + resync) | لا | نعم |
| **إرسال طلب** | offline queue في التطبيق + cart محفوظ محلياً | لا | نعم |
| **Broadcast** | BullMQ queue في الباك إند — retry ×3 تلقائي | لا | نعم |

## ٢) انتهاء Token

| الحالة | السلوك |
|---|---|
| access token منتهي | refresh تلقائي عبر refresh_token (1h/7d) — مُتحقق 201 |
| refresh منتهي | دخول جديد + OTP |
| refresh مُعاد استخدامه | **401 + قتل عائلة الجلسة** (كشف سرقة — مُتحقق) |
| جهاز مختلف | **401 device_mismatch** (مُتحقق) |

## ٣) إغلاق التطبيق فجأة / Restart / Background

| الحالة | السلوك |
|---|---|
| Kill أثناء مكالمة | إشعار missed call (push) + جلسة تُغلق بـ timeout (300s) |
| Kill أثناء طلب | الطلب محفوظ في MongoDB — يُستأنف من orders/mine |
| Restart جهاز | guest identity محفوظة (device-bound 90d) — نفس الضيف بعد إعادة التثبيت |
| Background | Socket heartbeat يستمر (interval) + push يغطي الغياب |
| تغيير شبكة (wifi↔4G) | Socket reconnect تلقائي (مُتحقق) |
| VPN/Roaming | TURN fallback عبر coturn (tcp 443) |
| Low Memory / Battery Saver | image cache LRU (expo-image memory-disk) + Hermes GC فعّال — لا memory leak في المستمعين (cleanup في useEffect — مُتحقق بالكود) |

## ٤) فشل الخدمات الخارجية

| الخدمة | السلوك عند الفشل |
|---|---|
| R2 معطل | fallback base64 — الرفع لا ينكسر أبداً (مُتحقق) |
| AI provider 429/فشل | **fallback تلقائي للمزود التالي** (مُتحقق: gemini×3 → openai×3 → groq ✓) |
| Redis معطل | in-memory fallback في RedisService |
| MongoDB معطل | restart policy + healthcheck + auto-recovery (مُتحقق بإعادة تشغيل كاملة) |
| Resend domain غير موثَّق | SES fallback (يُفعَّل بمفاتيح SES) |

## ٥) نقاط التحسين الموثقة (صدق)
1. رفع الروشتة: offline queue للملفات الكبيرة (حالياً يُعاد يدوياً) — مقترح تحسين
2. مكالمة عند ضعف شديد بالشبكة: downgrade تلقائي video→audio (LiveKit يدعمها — يُفعَّل عند البناء الإنتاجي)
3. Payment webhook idempotency: موجود بالتصميم (intent ids فريدة) — يُتحقق عند أول دفعة حقيقية
