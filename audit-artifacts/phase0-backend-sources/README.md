# Backend Phase 0 source handoff

These files are copied from `/home/ubuntu/nabdah_backend_work` after targeted tests, TypeScript, build, and boot verification. The backend workspace has no `.git` metadata, so this is a source handoff, not a backend commit or push. The migration runner is intentionally not wired into production boot or a real database; it requires repository review and Mongo temporary integration tests before activation.
