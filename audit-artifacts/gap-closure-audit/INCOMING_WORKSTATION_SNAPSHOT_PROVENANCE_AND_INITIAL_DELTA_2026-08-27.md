# Incoming workstation snapshot — provenance and initial static delta

**Incoming remote branch:** `quarantine/workstation-source-51a84c7`
**Remote snapshot commit:** `6d4d42fed4673f89961e0e403469ce1e3c5458dc`
**Snapshot-declared workstation source revision:** `51a84c76a690f30baac8b4bb3df6ab575aad4520`
**Baseline main:** `22526bedb77a3d8148219036367e4714f401aecc`

> This is a provenance checkpoint, not a readiness verdict. The incoming branch is a **one-commit isolated snapshot** with no shared Git ancestry with `main`; it cannot be reviewed as the claimed 63/67 commit history. Source-level comparison must use the supplied tree and a file-level baseline, while historical commit claims remain unverified unless the original repository objects or a branch carrying their ancestry are provided.

## Verified scope

| Check | Result | Consequence |
|---|---|---|
| Remote branch head | `6d4d42fe…` exists. | Snapshot can be source-read. |
| Git history | One root commit; `git merge-base main incoming` has no common ancestor. | No `main...incoming` commit-range review is valid. |
| Claimed hashes in snapshot docs | 50 unique abbreviated hashes extracted; 0 commit objects available in the snapshot Git object database. | Claims in supplied documents are data/claims, not verified commit evidence. |
| Tracked files | 1,971: backend 809; patient-mobile 631; patient-web 513; shared contracts 5. | Source scope is material for those included trees. |
| Provider app | Absent: `provider/SOURCE_MISSING.md` confirms broken-symlink target was excluded. | Provider source changes and tests cannot be verified. |
| Admin app | No Admin frontend/dashboard source directory exists; only backend Admin modules are present. | Admin UI claims cannot be verified. |
| Excluded files | Provider; `patient-mobile/GoogleService-Info.plist`; `patient-mobile/google-services.json`; all uncommitted changes including `packages/shared-contracts/src/patient-contracts.ts`. | Mobile integration configuration and uncommitted contracts are out of evidence scope. |
| Runtime | Snapshot manifest says no source was installed/imported/executed/tested when it was constructed. | No runtime/build/gate claim is accepted from snapshot presence. |

## Included-tree delta orientation

The trees are not Git-related. The `main` Git tree stores source archives, so this inventory compares their **audited extracted source directories** with the corresponding tracked directories in the incoming snapshot by byte hash. It does not use a commit range or infer functional closure from a filename/blob difference.

| Compared product source | Same bytes | Modified | Baseline only | Incoming only | Initial interpretation |
|---|---:|---:|---:|---:|---|
| Backend | 724 | 48 | 416 | 37 | Material code/test/ops delta plus a much smaller snapshot tree; review must distinguish deliberate omission, archive artifacts and feature loss. |
| Patient Mobile | 616 | 13 | 36 | 2 | Targeted changes; the excluded Firebase configuration prevents mobile integration closure. |
| Patient Web | 378 | 40 | 708 | 95 | Incoming Web tree is materially smaller than baseline despite new surfaces; every claimed route still needs CTA→contract review. |
| Provider frontend | 0 | 0 | 83 | 0 | Actual Provider source absent from incoming scope. |
| Admin frontend | 0 | 0 | 66 | 0 | Actual Admin UI source absent from incoming scope. |

The full **3,262-row** path-level machine-readable inventory is `INCOMING_WORKSTATION_FILE_LEVEL_TREE_DELTA_2026-08-27.tsv`. Its `semantic_interpretation` is intentionally `NOT_ASSESSED_BY_FILE_DELTA` for every row.

## Initial source findings requiring reconciliation

| Finding ID | Classification | Evidence | Why it remains open |
|---|---|---|---|
| SNAP-PHARM-001 | `CONFIRMED_DEFECT` | `backend/src/modules/payments/payments.module.ts:151–170` authorizes `booking.patient_id`; `pharmacy.schema.ts:110–135` defines required `patient_account_id`; `pharmacy-order.service.ts:46–57` creates only `patient_account_id`. | Patient payment-intent authorization for `pharmacy-order` has a concrete source field mismatch. It must be corrected and tested with patient owner/stranger/admin. |
| SNAP-PHARM-002 | `CONFIRMED_DEFECT` | `pharmacy-broadcast.service.ts:382–445` creates allocation, updates order, then separately attempts broadcast lock at 440–443 without checking update result; controller at `pharmacy.controllers.ts:31–35` accepts `pharmacy_account_id` only. | Concurrent selection can pass earlier reads before lock write; API request does not bind/verify a unique offer ID. Requires atomic compare-and-set/transaction, selected-offer binding, race tests and reservation reconciliation. |
| SNAP-WEB-003 | `STATIC_MATCHED_PARTIAL` | `patient-web/app/api/pharmacy/orders/route.ts:3–7` invokes `createAndSendOrder`; `lib/api/pharmacy-flow.ts:64–70` makes create then submit with no idempotency header. Conversely, the actually rendered card/wallet forms in `app/[locale]/pharmacy/pay/page.tsx:10–18,86–89` supply deterministic order/channel keys, while the unused helper at `pharmacy-flow.ts:84–87` generates a date-based key. | CSRF/auth/zod are present, but create-and-submit lacks explicit idempotency and combines two writes. No matching Web pharmacy route/replay test was found by static scan. Implement one durable operation key across create→submit/recovery and remove or cover the divergent helper. |
| SNAP-WEB-006 | `MISSING_CAPABILITY` | Patient-visible pharmacy flow is `app/[locale]/pharmacy/offers/page.tsx:37–72` and `app/[locale]/pharmacy/pay/page.tsx:64–91`; it renders offer selection and wallet/card forms only. A BFF COD endpoint exists at `app/api/pharmacy/orders/[orderId]/cod/route.ts:1–10`, but no pharmacy UI/CTA references it. | The required explicit COD/deferred-collection choice is not reachable through the inspected Patient Web pharmacy journey. Add informed COD selection, status/receipt/next-state and owner/stranger/replay coverage. |
| SNAP-WEB-007 | `MISSING_CAPABILITY` | Search of Patient Web pharmacy pages/components/BFF finds no pharmacy insurance request, pharmacy decision, co-pay or alternate/cancel surface; only generic insurance request pages and generic insurance BFF routes exist. `app/[locale]/pharmacy/pay/page.tsx:86–89` presents wallet and card only. | The required pharmacy insurance branch—post-offer internal decision, full/partial/reject/co-pay notification, patient co-pay payment or intentional alternate/cancel—is not demonstrated in the Web journey. It requires an explicit contract and actor workflow; generic insurance UI is not evidence of pharmacy fulfillment. |
| SNAP-PHARM-INS-008 | `CONFIRMED_DEFECT` | `backend/src/modules/insurance-engine/insurance-engine.module.ts:258–273` maps pharmacy to the pharmacy order model then looks it up with `{ id, patient_id: user.id }`; `pharmacy.schema.ts:112–114` and `pharmacy-order.service.ts:46–48` use `patient_account_id` instead. | A pharmacy insurance request cannot find the owner order through this path, so the required post-offer insurance branch is blocked server-side as well as absent in Web. Normalize ownership model fields and add pharmacy insurance request/decision/co-pay/cancel tests. |
| SNAP-INS-AUTHZ-009 | `CONFIRMED_DEFECT` | `insurance-engine.module.ts:537–538` exposes provider queue/decision under JWT only; service `providerQueue()` at 338–342 and `decide()` at 368–372 filter/match ID but do not assert an allowed provider role or provider eligibility. | Provider-only insurance decisions need an explicit verified provider role/entitlement guard in controller/service, independently of ID matching, with patient/admin/provider negative tests. |
| SNAP-MOB-PHARM-009 | `CONFIRMED_DEFECT` | `patient-mobile/app/pharmacy/checkout.tsx:99–114,310–340` presents cash/card/wallet/insurance selection. It constructs `payload.payment_method` at lines 136–157, but the actual governing create request at 165–176 sends only items, address, delivery mode, notes and prescription; it omits payment method, insurance, coupon, loyalty and wallet selections. | The UI represents payment/insurance choices that the actual broadcast-order request discards. Consequently the selected post-offer path cannot reliably follow the governing cash/COD/insurance state machine. Transmit only a contract-approved, server-validated selection—or remove/disable the controls—then test all branches. |
| SNAP-MOB-PHARM-010 | `STATIC_MATCHED_PARTIAL` | `patient-mobile/app/pharmacy/offers.tsx:39–55` implements explicit POST selection and has an offer comparison UI; `app/pharmacy/payment.tsx:73–85,247–270` exposes COD only after selection and card intent with `paymentIntentHeaders`. | Source shows an intended COD/card UX, but offer selection binds only `pharmacy_account_id`; it inherits the backend race/offer-binding defect. The pharmacy insurance state is rendered conditionally from order fields without a demonstrated request/decision contract in this slice. |
| SNAP-WEB-004 | `CONFIRMED_DEFECT` | `patient-web/app/[locale]/pharmacy/select/page.tsx:12–35` performs upstream `POST /select-offer` during Server Component rendering reached through query-based GET, with an httpOnly cookie; lines 7–10 merely assert this is CSRF-safe. | State-changing selection must occur only after an intentional protected POST/server action with CSRF/anti-replay behavior, not while handling a GET navigation/prefetch. Split the confirmation screen from the mutation and add browser-level tests. |
| SNAP-WEB-005 | `CONFIRMED_DEFECT` | `patient-web/app/[locale]/pharmacy/select/page.tsx:19,35` redirects to `/ar/login` and `/ar/pharmacy/pay` regardless of active `[locale]`. | Preserve/validate the active locale and test RTL/LTR/localization routes; current behavior breaks locale consistency. |
| SNAP-WEB-BOOK-012 | `CONFIRMED_DEFECT` | `patient-web/components-next/appointment-booking-form.tsx:14–33` promises cash payment now but redirects every returned `booking_id` to appointment detail. BFF `app/api/appointments/book/route.ts:76–78` accepts `pending_payment`; detail page `app/[locale]/appointments/[appointmentId]/page.tsx:31–42` renders cancel/reschedule/call actions only, with no payment-intent CTA. | The inspected cash consultation path permits the patient to land on a pending booking without a Web payment continuation, violating the required pay-before-confirmation journey. Add/route a guarded payment intent and success/failure/webhook recovery state before exposing confirmation/actions. |
| SNAP-MOB-QUALITY-011 | `STATIC_MATCHED_PARTIAL` | يوجد `@ts-nocheck` في 201 ملفًا من Patient Mobile، منها 10 من الملفات المعدلة في snapshot: `pharmacy/checkout.tsx`, `pharmacy/offers.tsx`, `pharmacy/payment.tsx`, diagnostics، nurse profile، payment success والإعدادات. | لا يمكن اعتبار نجاح فحص TypeScript المدعى به دليلاً على سلامة الأنواع لهذه الرحلات بينما فحصها معطّل. يجب إزالة التعطيل أو حصره بخطة دين تقني موثقة، وإضافة types/اختبارات لمسارات المال والحجز. |
| SNAP-TEST-011 | `INSUFFICIENT_EVIDENCE` | Snapshot contains 101 backend, 147 Patient Web and 34 Patient Mobile test/e2e-named files by static filename count, but zero Patient Web and zero Patient Mobile test filenames targeting pharmacy/offer/checkout/COD. | Test counts and green-gate statements do not establish the governed pharmacy journey. Require test manifests/results tied to current immutable source and explicit source-level owner/stranger/race/replay cases. |
| SNAP-PHARM-003 | `STATIC_MATCHED_PARTIAL` | `backend/src/modules/pharmacy/tests/pharmacy-broadcast.service.spec.ts:148–204,207–297` tests reject and automated best-partial allocation, not `patientSelectOffer`; `backend/e2e/governing-rules.js:29–50` performs only create, early-payment, offers-read and invalid-pharmacy sequential probes. | No source test covers two concurrent valid selections, conditional-lock failure recovery, selected offer ID binding, patient-owner versus stranger, or the card payment outcome. Its stated staging dependency is not execution evidence. |
| SNAP-CHAT-006 | `STATIC_MATCHED_PARTIAL` | `patient-web/components-next/chat-realtime.tsx:27–45` obtains a scoped token and joins a thread; `app/api/chat/threads/[threadId]/rt-token/route.ts:16–28` checks cookie authentication and proxies token mint; `backend/src/modules/chat/chat.gateway.ts:34–105` verifies JWT, audience/scope and `chatService.getThread()` membership. | Source supports a bounded realtime refresh path and participant check, but socket CORS/token expiry behavior remains runtime-required. No Provider UI is supplied to verify the peer workflow. |
| SNAP-CHAT-007 | `CONFIRMED_DEFECT` | `backend/src/modules/chat/chat.gateway.ts:125–145` uses caller-supplied `data.state` to prohibit closed-thread messages; it does not load server lifecycle state before relay. | A participant can send another state and cause a realtime relay in a closed thread. Enforce server-derived lifecycle state and test closed/follow-up/unauthorized socket cases. |
| SNAP-CHAT-008 | `STATIC_MATCHED_PARTIAL` | `backend/src/modules/chat/chat.gateway.ts:28–30` keeps connection/thread state in process-local `Map`s and states that a Redis adapter is required for multi-instance deployment. | Realtime presence/scope behavior is not horizontally scalable by source alone. Require a distributed adapter or compatible scaling strategy plus multi-instance tests. |
| SNAP-E2E-004 | `CONFIRMED_DEFECT` (portability/reproducibility) | `backend/e2e/run-all-gates.js:2–35` requires local ports and defaults Provider app to an absolute workstation path; Provider source is excluded. | Claimed all-gates result cannot be reproduced from supplied snapshot. |
| SNAP-E2E-005 | `STATIC_MATCHED_PARTIAL` | `backend/e2e/pharmacy-scenarios.js:1–11,23–36` contains a local absolute JWT dependency path, a fixed E2E JWT secret and direct local DB seed/cleanup. | Script may be a local harness, not a production secret exposure; it is not portable or sufficient as independent production evidence. Require environment-injected fixture identities and CI-safe harness. |

## Explicit non-findings

This document does not say that all claimed changes are wrong. It records only what the isolated snapshot can prove or fail to prove. It does not run builds, E2E, migrations, deployments, or live data operations.
