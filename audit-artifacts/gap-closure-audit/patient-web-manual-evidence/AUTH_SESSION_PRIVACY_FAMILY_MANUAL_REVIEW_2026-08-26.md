# Patient Web: Auth, session, privacy and family — manual CTA→contract review

## Scope and evidence boundary

This artifact reviews the current Web source snapshot at `1321902fd29dc05b1ef0454cd528ba8952a8fc81`. It covers the localized login and landing pages, auth BFF handlers, session/cookie model, protected profile/settings/family summaries, and source-tree evidence for missing auth surfaces. It does **not** test the upstream Backend, real cookies, session refresh, OTP delivery, 2FA, authorization, consent, or any live user/PHI.

| Web surface | Source evidence |
|---|---|
| Public landing and login | `app/[locale]/page.tsx:10–11`; `app/[locale]/login/page.tsx:9–10`; `components-next/login-form.tsx:16–53` |
| Password, OTP and 2FA BFF | `app/api/auth/login/route.ts:6–19`; `app/api/auth/otp/request/route.ts:5–13`; `app/api/auth/otp/verify/route.ts:5–18`; `app/api/auth/session/exchange/route.ts:5–22`; `app/api/auth/verify-2fa/route.ts:6–18` |
| Session, refresh, cookies and logout | `lib/auth/cookies.ts:3–11`; `lib/auth/session.ts:5–9`; `app/api/patient/[...path]/route.ts:9–11`; `app/api/auth/session/route.ts:5`; `app/api/auth/logout/route.ts:5` |
| Protected PHI-adjacent reads | `app/[locale]/profile/page.tsx:27–58`; `app/[locale]/settings/page.tsx:17–44`; `app/[locale]/family/page.tsx:20–44`; `lib/api/settings-server.ts:3–17` |

## CTA→contract findings

| ID | Surface/actor/CTA | Source chain | Classification | Precise finding and required closure evidence |
|---|---|---|---|---|
| PW-AUTH-001 | Unauthenticated patient; password submit, OTP request/verify/exchange, and 2FA submit | `components-next/login-form.tsx:16–38` calls BFF routes; BFF forwards `/auth/login`, `/auth/otp/request`, `/auth/otp/verify`, `/auth/session/exchange`, and `/auth/login/verify-2fa` at the paths above. Password/2FA BFF require a token pair before setting `nabd_access`/`nabd_refresh`/`nabd_device`. | `STATIC_MATCHED_PARTIAL` | The source establishes a plausible password/OTP/2FA handoff and client error states, but it does not prove upstream identity checks, OTP recipient binding/rate limits, one-time exchange behavior, 2FA enrolment/ownership, token rotation/revocation, or the actual cookies returned by Backend. Contract tests must cover valid/invalid/expired/replayed OTP, account enumeration, device binding, 2FA challenge ownership, and session issuance. |
| PW-AUTH-002 | OTP verification and session exchange | `app/api/auth/otp/verify/route.ts:10–18` accepts only `{ok:true, expires_in<=60}` and rewrites an upstream `set-cookie`; `session/exchange/route.ts:8–22` only forwards the `nabd_otp_exchange` cookie. | `RUNTIME_REQUIRED` | The source relies on an HTTP-only exchange cookie being supplied by upstream rather than exposing an exchange token in the client JSON. Source review cannot verify header multiplicity/attributes, expiry, path rewrite safety, consumption, replay prevention, or agreement with the deployed OTP contract. Inspect the Backend controller and exercise the one-time/expired/replay paths before closing. |
| PW-AUTH-003 | Web login completeness relative to reviewed Mobile auth routes | Only `app/[locale]/login/page.tsx` exists among localized auth-related pages; source-tree scan found no register, forgot/reset-password, social/OAuth, Apple/Google, guest, OTP standalone, terms, or privacy/auth-welcome UI/handler references. The available handlers are limited to login/logout/OTP/session/2FA. | `MISSING_CAPABILITY` | In this source snapshot, Web has no patient-facing registration, password recovery/reset, social sign-in, guest policy flow, legal terms/consent screen, or standalone OTP/welcome route corresponding to Mobile auth candidates PM-001 and PM-003–PM-009. This is a source-availability judgment, not a claim that Backend lacks those APIs. Product/security owners must decide which are required, then provide contracts, anti-enumeration/abuse policy, consent versioning, and recovery/account-linking states. |
| PW-AUTH-004 | Protected-page access and protected read proxy | `lib/auth/session.ts:5–9` gates pages solely on presence of `nabd_access`; profile/settings/family call upstream directly with that token. By contrast, `app/api/patient/[...path]/route.ts:9–11` refreshes only allowed `GET` reads once after upstream 401 and clears cookies if refresh is unavailable. | `STATIC_MATCHED_PARTIAL` | Session handling is not uniform across protected reads: server-rendered pages can redirect after upstream 401 while the BFF proxy has a one-retry refresh path. Source does not prove that this is unsafe, but it does establish different stale-session behavior. Define one server-side session-validation/refresh policy, protect every read/mutation consistently, and test stale access/valid refresh, invalid refresh, logout/revocation, role change, owner/stranger and network failure. |
| PW-AUTH-005 | Logout CTA/session invalidation | `app/api/auth/logout/route.ts:5` calls upstream best-effort and always returns `{success:true}` after clearing browser cookies, without checking upstream HTTP outcome. | `CONFIRMED_DEFECT` | The Web endpoint reports success even if upstream logout fails or returns a non-OK status. It confirms local cookie clearing only, not server-side refresh/token revocation. Return an explicit local-vs-server outcome or create a durable revocation/retry policy, then test offline/upstream error and replay behavior. |
| PW-PRIV-001 | Authenticated patient; settings privacy/security/storage/session summaries | `settings/page.tsx:17–44` reads four protected endpoints and renders cards; line 43 explicitly renders `readOnlyBoundary`. `lib/api/settings-server.ts:3–17` contains reads only. | `MISSING_CAPABILITY` | Privacy/security settings are summary-only in the current Web source. There is no CTA or mutation contract to change privacy sharing, 2FA, data controls, storage, session termination, consent, export, or deletion. A visible boolean does not prove a patient can exercise privacy rights or secure an account. Define legal/clinical/data-owner policy and auditable mutation contracts before building controls. |
| PW-PRIV-002 | Authenticated patient; session list | `settings/page.tsx:41` renders at most eight session summaries and no action controls. | `MISSING_CAPABILITY` | The Web has no source evidence for viewing all sessions, revoking a device/session, step-up confirmation, or a notification/result/audit trail for session termination. Backend session ownership and device identifiers must be reconciled before implementation. |
| PW-PHI-001 | Authenticated patient; profile PHI read | `profile/page.tsx:27–40, 53–58` reads profile, medical profile and insurance then renders selected identity/medical/insurance fields. `401` redirects at line 33; other non-OK outcomes become domain states through lines 15–19 and 40. | `STATIC_MATCHED_PARTIAL` | The page shows session-gated display and honest empty/forbidden/unavailable branches, but source does not establish resource ownership, field-level PHI authorization, data accuracy/freshness, audit logging, or edit/export/delete consent. Validate in Backend/Data by actor and purpose; test owner/stranger/unauth and minimal disclosure per field. |
| PW-FAM-001 | Authenticated patient; family group/member display | `family/page.tsx:20–44` fetches members/group and renders display name, role, relation and joined date only; no invite/edit/revoke/delegation CTA exists. | `MISSING_CAPABILITY` | The current Web family surface is read-only. It has no evidence for family-member invite, relationship validation, scoped PHI delegation, guardian consent, expiry/revocation, or audit/notification. Do not treat member display as a delegation workflow. |
| PW-AUTH-006 | Public landing welcome alternative | `app/[locale]/page.tsx:10–11` is a public landing page with an explicit login link and a medicine-catalog link. | `STATIC_MATCHED_PARTIAL` | Landing can be a web alternative to a native welcome surface, but it is not an equivalent onboarding/legal/account-choice flow. Any `NATIVE_ONLY_WITH_WEB_ALTERNATIVE` disposition requires an approved patient-goal mapping and a Web replacement for consent/account recovery/guest decisions where applicable. |

## Row-level mapping disposition for reviewed Mobile auth candidates

| Mobile row | Manual Web mapping | Current disposition |
|---|---|---|
| PM-001 forgot password | No localized Web route or auth handler located by tree/repository scan. | `MISSING_CAPABILITY` |
| PM-002 login | `app/[locale]/login/page.tsx` + `components-next/login-form.tsx`. | `STATIC_MATCHED_PARTIAL` |
| PM-003 OTP | Login-form OTP mode and OTP BFF handlers, not a separate route. | `STATIC_MATCHED_PARTIAL`; runtime contract required. |
| PM-004 privacy | `app/[locale]/settings/page.tsx` shows privacy summary only. | `MISSING_CAPABILITY` for consent/privacy management equivalence. |
| PM-005 provider info | No patient-Web auth/provider-info route identified in the reviewed source tree. | `INSUFFICIENT_EVIDENCE` for the Mobile screen goal; do not infer absence of a Backend profile contract. |
| PM-006 register | No localized Web registration route/handler located. | `MISSING_CAPABILITY` |
| PM-007 reset password | No localized Web reset route/handler located. | `MISSING_CAPABILITY` |
| PM-008 terms | No localized Web terms/acceptance route/handler located. | `MISSING_CAPABILITY` |
| PM-009 welcome | Public landing is a partial Web alternative; it lacks the evaluated Mobile onboarding/legal choices. | `STATIC_MATCHED_PARTIAL` |

## No runtime or Backend claims

No live endpoint, browser, device, build, test, deploy, migration, or account action was performed. The classifications above are bounded to source evidence. In particular, they do not claim that an upstream route works or fails, that a cookie is secure in deployment, or that a particular patient can access PHI.
