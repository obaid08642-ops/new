# Semantic evidence — Mobile Pharmacy Chat with Pharmacist

Baseline: `22526bedb77a3d8148219036367e4714f401aecc`.

`audit-work/source/nabd_plus_patient_app/app/pharmacy/chat-with-pharmacist.tsx:35–80` creates a booking-thread context with `POST /chat/threads/booking` using `orderId` and `booking_kind: pharmacy`, then reads thread messages. However, `orderId` is only a route parameter and the source does not visibly validate format, ownership, order state or pharmacy membership. The pharmacist header starts with a default object and marks the pharmacy “متصل” when an order pharmacy name exists (`:24–28,49–58`), without a presence/realtime contract. Thread/message failures are swallowed (`:53–79`), leaving no user-facing unavailable, forbidden, not-found or retry state.

The 15-minute session is a local countdown initialized on mount (`:40–42,82–103`) rather than a server-issued expiry/lease; it can expire locally without proving server closure, renewal, timezone or reconnect behavior. Messages are appended optimistically with `String(Date.now())` IDs (`:111–135`), posted without visible Idempotency-Key, request correlation, membership/consent or rate-limit controls, and only later marked failed if a thread exists. There is no retry action, delivery/read status, moderation or PHI policy. Attachments use native camera/library and upload to `/media/upload` followed by a message POST (`:137–181`) without visible file size/type/content/PHI/retention controls, idempotency, cleanup or server attachment identity; failed sends remain locally present as failed bubbles.

Price cards contain item name/quantity/price/fee/total data but no visible server schema/currency/freshness/order binding (`:199–232`). “Confirm and add to cart” only navigates to local `/pharmacy/cart` (`:183–197`), while “accept alternatives”, “remove unavailable items” and “cancel order” show success alerts or navigate back without mutations (`:234–266`). These controls do not prove server order state, substitution consent, price/stock reconciliation, cancellation, refund or audit semantics. The pharmacy status and price data are rendered as trusted message content without ownership or PHI minimization. No Phase 0 remediation was made.
