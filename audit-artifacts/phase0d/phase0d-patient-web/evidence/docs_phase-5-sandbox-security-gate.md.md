# Phase 0D semantic evidence

- **Surface:** Patient Web
- **Archive:** `nabd-patient-web.zip`
- **Member path:** `docs/phase-5-sandbox-security-gate.md`
- **Member SHA-256:** `c20c5b5e3dd7f642dd0a2cf48c26cf54fb065471ded439f8624a570ab00463a2`
- **Line count:** 101
- **Read range:** `1-101`
- **Classification:** `OWNED_SOURCE_OR_CONFIG`

## Static semantic observations

This evidence is derived from the complete decoded member bytes in the baseline archive. It is not runtime proof and no product code was executed or changed.

### routes_screens_actions
- `5: أجري الاختبار بحسابَي Sandbox المعتمدين فقط، وبعمليات `POST /auth/login` و`GET /orders/{id}` للقراءة. لم يُنشأ طلب، ولم تُجر دفعة أو إلغاء أو تعديل أو رفع ملف. لا يحتوي هذا المستند على بريد أو كلمة مرور أو توكن أو معرّف مورد.`
- `13: | عدم وجود أثر تعديل | ناجح | الاختبارات محصورة في login وGET. |`
- `23: نجح اختبار HTTP المحلي للمسار الخاص عبر `POST /api/auth/login` وcookies الخادمية: عرضت صفحة تفاصيل الطلب المورد المصرح به للمالك بـ`200`، وعرضت صفحة `404` المترجمة للحساب المختلف بدلاً من أي كشف للطلب. لم يطبع الاختبار cookies أو كلمات مرور`
- `41: نجح اختبار Sandbox القرائي لمسار `GET /home-care/bookings/my` مع تسجيل دخول المالك والحساب المختلف من دون تنفيذ حضور أو تتبع أو تقرير أو تعديل حجز. يتحقق الاختبار من قراءة قائمة المالك، ويختبر تفاصيل مورد فعلي ضد الحساب المختلف عندما يتوافر`
- `45: اجتازت بوابة `verify-core-routes.sh` المسارات العامة العربية والإنجليزية وتسجيل الدخول وصفحة `404`، كما أكدت أن `GET /api/auth/session` و`GET /api/patient/orders/mine` يعيدان `401` من دون جلسة، وأن `/ar/profile` يعيد التوجيه إلى الدخول. أظه`
- `101: أضيفت سياسة `X-Robots-Tag: noindex, nofollow, noarchive` في وسيط الويب لكل مسار HTML غير عام، بما يشمل صفحة الدخول وكل لوحات ومسارات المريض. تستثنى حصراً صفحات البداية العامة الست `/ar` و`/en` و`/ur` و`/hi` و`/bn` و`/fil` كي لا تتعارض سياسة`
### backend_consumers_or_contracts
- `5: أجري الاختبار بحسابَي Sandbox المعتمدين فقط، وبعمليات `POST /auth/login` و`GET /orders/{id}` للقراءة. لم يُنشأ طلب، ولم تُجر دفعة أو إلغاء أو تعديل أو رفع ملف. لا يحتوي هذا المستند على بريد أو كلمة مرور أو توكن أو معرّف مورد.`
- `19: يجوز بناء واجهة **عرض الطلب الخاص** التالية فوق عقد `GET /orders/{id}`، بشرط أن يظل استدعاؤها عبر BFF الخادمي، وأن تُحافظ الواجهة على حالات `401` و`403/404` الصريحة دون استبدالها بنتيجة محلية. لا يعتبر هذا اعتماداً لتدفقات الإلغاء أو إعادة `
- `23: نجح اختبار HTTP المحلي للمسار الخاص عبر `POST /api/auth/login` وcookies الخادمية: عرضت صفحة تفاصيل الطلب المورد المصرح به للمالك بـ`200`، وعرضت صفحة `404` المترجمة للحساب المختلف بدلاً من أي كشف للطلب. لم يطبع الاختبار cookies أو كلمات مرور`
- `25: امتد الاختبار إلى قائمة `/ar/orders`: عرضت قائمة المالك المورد الاختباري، بينما لم تعرض قائمة الحساب المختلف معرّف هذا المورد. بقيت جميع الطلبات اختبارية وقرائية فقط، ولا تُعتبر هذه النتيجة اعتماداً لأي عملية تعديل.`
- `29: تحققت عقود `GET /users/me/profile` و`GET /medical-profile` و`GET /users/me/insurance` للحساب المالك دون خطأ تفويض. ثم نجح اختبار Web App لصفحة `/ar/profile` للحسابين، مع التحقق من عدم ظهور بريد المالك في صفحة الحساب المختلف. تعرض الواجهة حق`
- `37: نجح اختبار Sandbox في قراءة قائمة `GET /care/appointments` للحساب المالك دون تنفيذ أي إنشاء أو إلغاء أو إعادة جدولة. كما يتحقق اختبار الويب من أن صفحة `/ar/appointments` محمية بالجلسة، وأن معرف الموعد غير الصالح يعرض `404`، وأن HTML لا يعرض`
- `41: نجح اختبار Sandbox القرائي لمسار `GET /home-care/bookings/my` مع تسجيل دخول المالك والحساب المختلف من دون تنفيذ حضور أو تتبع أو تقرير أو تعديل حجز. يتحقق الاختبار من قراءة قائمة المالك، ويختبر تفاصيل مورد فعلي ضد الحساب المختلف عندما يتوافر`
- `45: اجتازت بوابة `verify-core-routes.sh` المسارات العامة العربية والإنجليزية وتسجيل الدخول وصفحة `404`، كما أكدت أن `GET /api/auth/session` و`GET /api/patient/orders/mine` يعيدان `401` من دون جلسة، وأن `/ar/profile` يعيد التوجيه إلى الدخول. أظه`
- `65: في إعادة التحقق القرائية، نجحت مجالات `GET /users/me/profile` و`GET /medical-profile` و`GET /users/me/insurance` لحساب Sandbox المالك. كما أعاد كل مسار `401` عند الطلب بلا `Authorization`، وذلك في اختبار لا يطبع أي body أو token أو بيانات ا`
- `85: نجحت القراءة القرائية لقائمة `GET /notifications` وعدد غير المقروء `GET /notifications/unread-count` لحساب Sandbox المالك، وأعاد كلا المسارين `401` بلا جلسة. اختبر هذا بعد عودة اتصال Sandbox من مهلة اتصال عابرة، ومن دون إعادة محاولة تلقائية`
### auth_ownership
- `5: أجري الاختبار بحسابَي Sandbox المعتمدين فقط، وبعمليات `POST /auth/login` و`GET /orders/{id}` للقراءة. لم يُنشأ طلب، ولم تُجر دفعة أو إلغاء أو تعديل أو رفع ملف. لا يحتوي هذا المستند على بريد أو كلمة مرور أو توكن أو معرّف مورد.`
- `9: | صحة بيانات حساب Sandbox المالك | ناجح | اختبار `sandbox-secrets.test.ts` تحقق من تسجيل الدخول ووجود access token دون طباعته أو تخزينه. |`
- `10: | الوصول المصرح به | ناجح | اختبار `sandbox-order-ownership.test.ts` استقبل `200` عند قراءة المورد من حساب المالك. |`
- `12: | عدم كشف الأسرار | ناجح | جميع البيانات من متغيرات البيئة، والاختبارات لا تطبع token أو كلمة مرور أو body للاستجابة. |`
- `13: | عدم وجود أثر تعديل | ناجح | الاختبارات محصورة في login وGET. |`
- `14: | تدوير الجلسة الخادمي | ناجح | اختبار BFF محلي يثبت أن `401` يرسل `refresh_token` مرة واحدة فقط، ثم يعيد المحاولة بالتوكن المدور ويحدّث cookies الخادمية بالتوكنين العائدين فقط. |`
- `15: | إنهاء الجلسة عند فشل refresh | ناجح | الاختبار نفسه يثبت أن غياب refresh أو device، أو استجابة refresh الناقصة، لا تنشئ جلسة بديلة وتؤدي إلى تنظيف cookies عند بقاء `401`. |`
- `23: نجح اختبار HTTP المحلي للمسار الخاص عبر `POST /api/auth/login` وcookies الخادمية: عرضت صفحة تفاصيل الطلب المورد المصرح به للمالك بـ`200`، وعرضت صفحة `404` المترجمة للحساب المختلف بدلاً من أي كشف للطلب. لم يطبع الاختبار cookies أو كلمات مرور`
- `45: اجتازت بوابة `verify-core-routes.sh` المسارات العامة العربية والإنجليزية وتسجيل الدخول وصفحة `404`، كما أكدت أن `GET /api/auth/session` و`GET /api/patient/orders/mine` يعيدان `401` من دون جلسة، وأن `/ar/profile` يعيد التوجيه إلى الدخول. أظه`
- `49: نجح تسجيل دخول Sandbox الخادمي في المتصفح بحالة `200` وباستجابة عامة مقتصرة على تأكيد المصادقة. بعده، عرضت `/ar/dashboard` روابط الميزات القرائية المؤهلة فقط، من دون توكن أو بريد أو معرّف مريض خام أو بيانات صحية. يظل اختبار نماذج الدخول الت`
- `61: أضيفت طبقة تحميل وخطأ عامة مترجمة، وزر إعادة محاولة خادمي مقيد لحالات تعذر القراءة. لا يظهر الزر في حالة `403/404`، فلا يتحول رفض التفويض إلى كشف وجود مورد أو مسار بديل. أثبت اختبار SSR للملف الصحي عدم تسلسل token أو مفتاح تخزين أو ملاحظة س`
- `65: في إعادة التحقق القرائية، نجحت مجالات `GET /users/me/profile` و`GET /medical-profile` و`GET /users/me/insurance` لحساب Sandbox المالك. كما أعاد كل مسار `401` عند الطلب بلا `Authorization`، وذلك في اختبار لا يطبع أي body أو token أو بيانات ا`
### state_transitions
- `69: ثبت اختبار Sandbox لاحق أن `medical-profile` يوفر حقلاً واحداً على الأقل من allowlist الويب بحيث يصنف المجال `available` ويصبح قابلاً للعرض، بينما تقبل مجالات الهوية والتأمين حالتي `available` أو `empty` فقط عند نجاح الاستجابة، من دون إنشاء`
### payment_insurance_relevance
- `29: تحققت عقود `GET /users/me/profile` و`GET /medical-profile` و`GET /users/me/insurance` للحساب المالك دون خطأ تفويض. ثم نجح اختبار Web App لصفحة `/ar/profile` للحسابين، مع التحقق من عدم ظهور بريد المالك في صفحة الحساب المختلف. تعرض الواجهة حق`
- `65: في إعادة التحقق القرائية، نجحت مجالات `GET /users/me/profile` و`GET /medical-profile` و`GET /users/me/insurance` لحساب Sandbox المالك. كما أعاد كل مسار `401` عند الطلب بلا `Authorization`، وذلك في اختبار لا يطبع أي body أو token أو بيانات ا`
### error_empty_loading_retry_cancel
- `69: ثبت اختبار Sandbox لاحق أن `medical-profile` يوفر حقلاً واحداً على الأقل من allowlist الويب بحيث يصنف المجال `available` ويصبح قابلاً للعرض، بينما تقبل مجالات الهوية والتأمين حالتي `available` أو `empty` فقط عند نجاح الاستجابة، من دون إنشاء`

## Required verification boundary

Validate the extracted routes/actions against the live backend contract, authorization matrix, state machine, payment/insurance source of truth, and UI error/loading/empty/retry/cancel behavior before treating this surface as production-ready.
