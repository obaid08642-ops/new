# Phase 0B semantic evidence — User schema

**Baseline:** `main @ 22526bedb77a3d8148219036367e4714f401aecc`

**Member read in full:** `src/schemas/user.schema.ts:1–47`

The timestamped `users` schema defines generated ID, full name, sparse unique phone/email, optional password hash, runtime `UserRole` enum defaulting patient, active/guest/deleted state, avatar/city/district/location, preferred language, embedded legal consents, sparse unique/indexed health_id, device tokens, last login, institution staff parent/branch/department/permissions/schedule/suspended/verified fields, and doctor specialty/degree/experience/license/consultation_fee (`6–47`). Explicit controls include unique sparse contact/health identifiers, role enum, consent tuple shape and reference indexes (`10–13,23–26,30–38`). Comments state consent evidence is immutable and health_id must be the client-facing identifier (`22–26`), but comments are not enforcement.

Full name/contact/language/location and professional fields have no visible normalization, length, format, consent, encryption, field-level access or PII minimization policy (`9–21,40–44`). `password_hash` has no algorithm/version/rotation/compromise/reset metadata or explicit select:false protection in this member (`12`). Device tokens are an unbounded array with no platform/owner/expiry/revocation/dedup controls (`27`). Legal consent entries are structurally typed but have no actor, source, jurisdiction, withdrawal/revocation, policy content hash or temporal ordering guard (`23–24`). Location has no coordinate range/accuracy/timestamp/retention control (`20`). Cross-field invariants are absent: guest/contact/password, role/staff parent/branch/permissions, doctor licensing, active/suspended/deleted/verified and health_id eligibility are not constrained (`13–16,30–44`). `schedule` is `any`, years_experience and consultation_fee are unconstrained, and fee has no currency/calculation/payment linkage (`36,42,44`). No code was changed and no build/test/application operation was performed during this read.
