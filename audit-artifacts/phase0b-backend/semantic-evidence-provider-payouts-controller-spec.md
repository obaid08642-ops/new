# Phase 0B semantic evidence — ProviderPayoutsController spec

**Baseline:** `main @ 22526bedb77a3d8148219036367e4714f401aecc`

**Member read in full:** `src/modules/payouts/provider-payouts.controller.spec.ts:1–52`

The spec creates mocked Mongo collections for provider withdrawals, platform ledger entries, provider bank accounts and finance configuration, plus a mocked transaction session (`4–23`). It verifies that a missing approved bank account rejects before starting a transaction or inserting a withdrawal (`25–32`). It verifies a pre-existing withdrawal for the same idempotency key is returned without a second withdrawal or ledger reservation (`34–40`). It verifies an approved bank account plus an aggregate balance creates one PENDING_ADMIN_APPROVAL withdrawal and one locked payout ledger reservation in the same mocked transaction, with available balance 300 from cleared earnings 500 less amount 200 (`42–51`).

This is focused reservation-contract coverage, but the controller is called directly and all Mongo/finance dependencies are mocked (`12–23`). It does not prove HTTP authentication, provider identity/tenant ownership, account active/verified state, admin approval authorization or bank-account ownership (`25–51`). Amount validation is not tested for zero, negative, fractional precision, excessive amount, currency, minimum payout or available-balance edge cases; finance_config is configured but its minimum threshold is not asserted (`16,42–49`). Ledger aggregation, pending/paid/debit/locked semantics, concurrent withdrawals, unique idempotency index, stale locks, transaction abort/rollback or ledger reconciliation are not proven (`34–50`). Bank IBAN format, sensitive-field projection, masking/encryption, bank change approval and payout destination verification are untested (`42–49`). Approval, rejection, settlement, retry, refund/chargeback, notification and audit lifecycle are absent. No PII/financial retention, access audit, rate limit, duplicate-key/error mapping or live database/payment/bank evidence exists. No code was changed and no build/test/application operation was performed during this read.
