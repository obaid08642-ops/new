# Phase 0B semantic evidence — LoyaltyService spec

**Baseline:** `main @ 22526bedb77a3d8148219036367e4714f401aecc`

**Member read in full:** `src/modules/loyalty/loyalty.service.spec.ts:1–129`

The spec builds LoyaltyService with mocked account, transaction, challenge, progress, reward and claim repositories (`5–58`). It verifies booking_completed awards 50 points and can create a missing account (`60–71`). It then calls the stated 500-point tier boundary with 450 existing lifetime points plus 50 awarded, but asserts the result is still bronze (`73–81`), making the test expectation itself inconsistent with its comment if silver is intended at 500. It verifies an unknown action awards zero (`83–86`). It verifies a reward claim deducts points and creates a claim record for an active reward with stock, and rejects insufficient points (`89–117`). It verifies a mocked leaderboard result has platinum tier (`119–127`).

All repositories are mocks, so no live account/transaction/reward ownership, user status, tenant or HTTP authorization is established (`14–58`). The `findOne` null idempotency fixture and daily-cap count fixture are configured but no duplicate transaction/replay or cap behavior is actually asserted (`21–27`). Point award coverage does not prove source-event authenticity, one-award-per-event, negative/overflow protection, atomic account+ledger update, tier boundary correctness, concurrent awards, reversal/refund or audit provenance (`60–86`). The 500 boundary expectation appears semantically contradictory (`73–81`). Reward claim coverage does not test reward ownership/eligibility, exact atomic points deduction, stock decrement race, duplicate claim, coupon uniqueness, expiry, fulfillment, refund or transaction rollback (`89–117`). Leaderboard coverage is one pre-shaped record and does not test authorization, pagination/rank ties, deleted users, PII projection or ordering query (`119–127`). No retention, fraud/abuse/rate-limit, notifications, external benefit fulfillment or live persistence evidence exists. No code was changed and no build/test/application operation was performed during this read.
