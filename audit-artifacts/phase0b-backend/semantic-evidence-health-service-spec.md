# Phase 0B semantic evidence — HealthService spec

**Baseline:** `main @ 22526bedb77a3d8148219036367e4714f401aecc`

**Member read in full:** `src/modules/health/health.service.spec.ts:1–171`

The medication-reminder section constructs HealthService with mocked reminder, order, user and other collaborators (`4–27`). It verifies patient-owned reminder creation with IANA timezone, HH:mm normalization/deduplication and daily frequency (`29–48`), rejects non-24-hour time/invalid timezone and unsupported monthly recurrence while preserving a fractional dose (`50–70`), enforces one outcome per configured dose time and local day (`72–84`), preserves a past local-notification occurredAt/source and rejects future time (`86–98`), and rejects logging a time not configured on the reminder (`100–104`). It verifies refill creates one server order identity behind a conditional lock, omits fake pills_remaining/refill_date claims and records fulfillment pending (`106–126`), and reuses an existing pending refill order (`128–134`). The vital section uses a mock vitals model and verifies legacy sugar/heart aliases normalize to glucose/heart_rate with canonical units, listVitals maps sugar and rejects an unknown type (`137–170`).

The tests establish useful timezone, duplicate-dose, future-event and refill-lock intent, but all collaborators are mocks and no HTTP authentication or live patient ownership is exercised (`4–27,72–134`). Reminder coverage does not test medicine/catalog identity, dosage ranges/units, prescription safety, start/end lifecycle, recurrence semantics beyond selected cases, notifications, consent, PII/clinical retention or timezone DST behavior (`29–104`). Refill lock assertions do not prove atomic order/reminder transactionality, lock expiry/recovery, concurrent acquisition, order failure compensation, stock/price/address truthfulness, idempotency replay or cancellation (`106–134`). Vital tests do not validate clinical ranges, units, source/device trust, timestamps, abnormal-value escalation, duplicate handling, tenant/PHI access, deletion or audit (`155–170`). No rate limit, error mapping beyond selected exceptions, cache/event consistency or live Mongo evidence exists. No code was changed and no build/test/application operation was performed during this read.
