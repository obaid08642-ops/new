// Firebase background messaging is handled at native layer.
// This file is intentionally kept minimal — console logging removed.

export const initializeForegroundMessaging = () => {
  return () => {};
};
